---
phase: 02-lead-pipeline
plan: "05"
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - apps/web/.env.example
  - api/.env.example
autonomous: false
requirements: [LAND-02, GHL-01, GHL-02]
must_haves:
  truths:
    - "The Next.js app compiles with zero TypeScript errors"
    - "The FastAPI app starts without import errors"
    - "All new environment variables are documented in .env.example files"
    - "The landing page renders at /[clientSlug]/[service] with form, CTA, and trust signals"
  artifacts:
    - path: "apps/web/.env.example"
      provides: "Updated env var documentation for Phase 2"
      contains: "GHL_PRIVATE_TOKEN"
    - path: "api/.env.example"
      provides: "Updated env var documentation for Phase 2"
      contains: "GHL_ED25519_PUBLIC_KEY"
  key_links: []
---

<objective>
Verify the full Phase 2 integration: TypeScript compilation, FastAPI startup, env var documentation, and visual landing page check.

Purpose: Ensure all 4 prior plans wire together correctly before marking the phase complete.
Output: Updated .env.example files + verified builds
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-lead-pipeline/02-CONTEXT.md
@.planning/phases/02-lead-pipeline/02-01-SUMMARY.md
@.planning/phases/02-lead-pipeline/02-02-SUMMARY.md
@.planning/phases/02-lead-pipeline/02-03-SUMMARY.md
@.planning/phases/02-lead-pipeline/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update env.example files and verify builds</name>
  <files>apps/web/.env.example, api/.env.example</files>
  <read_first>
    - apps/web/.env.example (current env vars — add Phase 2 additions)
    - api/.env.example (current env vars — add Phase 2 additions)
    - apps/web/src/app/api/leads/submit/route.ts (check which env vars it uses)
    - api/routers/webhooks.py (check which env vars it uses)
    - apps/web/src/lib/ghl.ts (check which env vars it uses)
  </read_first>
  <action>
1. Update `apps/web/.env.example` — add these new env vars below the existing Supabase ones (keep existing vars, only add new):

```
# --- Phase 2: Lead Pipeline ---
# GoHighLevel
GHL_PRIVATE_TOKEN=              # GHL Private Integration Token (per sub-account, NOT NEXT_PUBLIC_)
GHL_SMS_WORKFLOW_ID=            # ID of pre-configured GHL SMS workflow for lead follow-up

# Resend
RESEND_API_KEY=                 # Resend API key for transactional email
RESEND_FROM_EMAIL=leads@tradeflow.io  # From address for lead notification emails

# App URL (used in email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

2. Update `api/.env.example` — add the GHL webhook-related var (keep existing vars, only add new):

```
# --- Phase 2: GHL Webhook ---
# Note: GHL Ed25519 public key is hardcoded in api/services/ghl_service.py
# No additional env vars needed for webhook verification
```

3. Run TypeScript compilation check:
```bash
cd apps/web && npx tsc --noEmit
```
Fix any TypeScript errors that arise from the new files.

4. Run FastAPI import check:
```bash
cd /Users/user/Desktop/Tradeflow && python -c "from api.main import app; print('FastAPI app loaded ok')"
```
Fix any Python import errors.

5. Verify all key files exist and have expected exports:
```bash
# Landing page has generateStaticParams
grep -q "generateStaticParams" apps/web/src/app/\(landing\)/\[clientSlug\]/\[service\]/page.tsx

# Lead form is a client component
grep -q "'use client'" apps/web/src/components/lead-form.tsx

# Lead submit route exports POST
grep -q "export async function POST" apps/web/src/app/api/leads/submit/route.ts

# Webhook router is registered
grep -q "include_router.*webhooks" api/main.py
```
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow && grep -q "GHL_PRIVATE_TOKEN" apps/web/.env.example && grep -q "RESEND_API_KEY" apps/web/.env.example && grep -q "GHL_SMS_WORKFLOW_ID" apps/web/.env.example && cd apps/web && npx tsc --noEmit 2>&1 | tail -5 && cd /Users/user/Desktop/Tradeflow && python -c "from api.main import app; print('ok')" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/.env.example contains GHL_PRIVATE_TOKEN, GHL_SMS_WORKFLOW_ID, RESEND_API_KEY, RESEND_FROM_EMAIL, NEXT_PUBLIC_APP_URL
    - `cd apps/web && npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - `python -c "from api.main import app"` exits with code 0 (no import errors)
    - api/main.py includes both health and webhooks routers
  </acceptance_criteria>
  <done>Both apps compile/import cleanly. All new env vars are documented in .env.example files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of landing page and lead pipeline</name>
  <files>apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx</files>
  <action>
Human verifies the complete Phase 2 lead pipeline by visually inspecting the landing page and testing the form submission flow.

What was built:
- Static landing pages at /[clientSlug]/[service] with hero, click-to-call CTA, lead form, and trust signals
- Lead capture form with 4 fields (name, phone, service type, zip code) + Zod validation
- Lead submit API route at /api/leads/submit with Supabase insert, GHL contact creation, SMS workflow trigger, Resend email
- GHL webhook endpoint at FastAPI /api/webhooks/ghl with Ed25519 signature verification
- Database migration adding slug column to clients table

Steps to verify:
1. Start the Next.js dev server: `cd apps/web && npm run dev`
2. Visit http://localhost:3000/oak-park-hvac/ac-repair (or whatever the seed client slug resolves to)
3. Verify:
   - Page shows "AC Repair in [city]" hero headline
   - Green click-to-call button is visible with phone number
   - Lead form has 4 fields: Name, Phone, Service Type (pre-filled "AC Repair"), Zip Code
   - Trust signals section shows star rating and service area
   - No navigation menu is present — single page with one CTA
4. Apply the SQL migration (002_add_slug_to_clients.sql) to your Supabase project
5. Set GHL_PRIVATE_TOKEN, GHL_SMS_WORKFLOW_ID, RESEND_API_KEY in .env.local
6. Submit the lead form and verify in Supabase dashboard that a lead record was created
7. Start FastAPI: `cd /path/to/repo && uvicorn api.main:app --port 8002`
8. Verify `/docs` shows the `/api/webhooks/ghl` endpoint
  </action>
  <verify>Human visually confirms the landing page layout, form fields, and lead submission flow.</verify>
  <done>Landing page renders correctly with all required sections. Lead form submits and creates Supabase record. FastAPI webhook endpoint visible at /docs.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with zero errors
- FastAPI imports succeed without errors
- Landing page renders visually correct with hero, CTA, form, trust signals
- Lead form submits and creates Supabase record
- GHL webhook endpoint appears in FastAPI /docs
</verification>

<success_criteria>
Both apps build and run cleanly. The landing page displays correctly with all required sections. The lead form submission flow works end-to-end (manual verification). All environment variables are documented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-lead-pipeline/02-05-SUMMARY.md`
</output>
