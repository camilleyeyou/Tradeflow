---
phase: 03-client-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07
  - DASH-08

must_haves:
  truths:
    - "All 4 dashboard pages render without errors"
    - "TypeScript and Next.js build pass cleanly"
    - "Dashboard is usable on a 375px mobile screen"
    - "Auth guard prevents unauthenticated access"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete dashboard builds cleanly and visually verify mobile responsiveness and full functionality.

Purpose: This is the final quality gate before marking Phase 3 complete. The build verification catches any TypeScript or import errors across all plans, and the visual checkpoint confirms the mobile-first layout works as intended for HVAC owners checking their phone between jobs.

Output: Confirmed working dashboard with passing build and visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-client-dashboard/03-CONTEXT.md
@.planning/phases/03-client-dashboard/03-01-SUMMARY.md
@.planning/phases/03-client-dashboard/03-02-SUMMARY.md
@.planning/phases/03-client-dashboard/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full TypeScript check and Next.js build</name>
  <files></files>
  <read_first>
    apps/web/package.json
    apps/web/tsconfig.json
  </read_first>
  <action>
Run the following checks from the `apps/web/` directory:

1. TypeScript compilation check:
```bash
npx tsc --noEmit
```
Fix any type errors that appear. Common issues to watch for:
- Supabase query results needing casts to local types (Lead, Call, Client)
- shadcn/ui component prop mismatches
- Missing imports between dashboard components

2. Next.js production build:
```bash
npm run build
```
Fix any build errors. This catches:
- Server/Client Component boundary issues (using hooks in Server Components, using `force-dynamic` in Client Components)
- Missing exports or broken imports
- Server Action compilation issues

3. Verify all dashboard routes are included in the build output. The build log should list:
- `/(dashboard)` (overview page)
- `/(dashboard)/leads`
- `/(dashboard)/calls`
- `/(dashboard)/settings`

All four should show as `f` (force-dynamic) in the build output, NOT `○` (static) or `●` (SSG).

If any route shows as static, that means `export const dynamic = 'force-dynamic'` is missing -- add it to the page.tsx file.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - Build output shows /(dashboard) route as dynamic (f)
    - Build output shows /(dashboard)/leads route as dynamic (f)
    - Build output shows /(dashboard)/calls route as dynamic (f)
    - Build output shows /(dashboard)/settings route as dynamic (f)
    - No TypeScript errors in build output
  </acceptance_criteria>
  <done>
    TypeScript compilation passes with zero errors.
    Next.js production build succeeds.
    All 4 dashboard routes are force-dynamic in the build output.
    No Server/Client Component boundary violations.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard on mobile and desktop</name>
  <files></files>
  <read_first>
    apps/web/src/app/(dashboard)/page.tsx
    apps/web/src/app/(dashboard)/leads/page.tsx
    apps/web/src/app/(dashboard)/calls/page.tsx
    apps/web/src/app/(dashboard)/settings/page.tsx
  </read_first>
  <action>
Start the dev server (`cd apps/web && npm run dev`) and present the dashboard to the user for visual verification. The user will check desktop layout, mobile layout at 375px, and auth guard behavior across all 4 pages.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npm run build 2>&1 | grep -c "error" | xargs test 0 -eq && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms desktop sidebar navigation works across all 4 pages
    - User confirms mobile bottom tab bar appears at 375px width
    - User confirms count cards show 2x2 grid on mobile
    - User confirms no horizontal scroll on mobile
    - User confirms auth redirect works for unauthenticated users
  </acceptance_criteria>
  <done>
    User has visually approved the dashboard layout on both desktop and mobile.
    All 4 pages render correctly with responsive behavior.
    Auth guard confirmed working.
  </done>
</task>

</tasks>

<verification>
- Next.js build passes with all routes dynamic
- Visual inspection confirms mobile layout at 375px
- Auth guard redirects unauthenticated users
</verification>

<success_criteria>
The dashboard builds cleanly, all routes are force-dynamic, the responsive layout works at 375px, and the user visually confirms the dashboard looks and functions correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-client-dashboard/03-04-SUMMARY.md`
</output>
