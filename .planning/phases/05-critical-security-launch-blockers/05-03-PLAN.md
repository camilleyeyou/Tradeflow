---
phase: 05-critical-security-launch-blockers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api/services/ghl_service.py
  - api/routers/webhooks.py
autonomous: true
requirements: [SEC-03]
must_haves:
  truths:
    - "The GHL webhook rejects (401) any request without a valid Ed25519 X-GHL-Signature"
    - "A request bearing only a legacy X-WH-Signature is no longer accepted"
    - "The always-true verify_ghl_legacy_signature function no longer exists in the code"
  artifacts:
    - path: "api/services/ghl_service.py"
      provides: "Ed25519 verification only (legacy bypass removed)"
      contains: "verify_ghl_ed25519_signature"
    - path: "api/routers/webhooks.py"
      provides: "GHL webhook that only accepts Ed25519, else 401"
      contains: "verify_ghl_ed25519_signature"
  key_links:
    - from: "webhooks.py ghl_webhook"
      to: "verify_ghl_ed25519_signature"
      via: "401 raised on missing/invalid X-GHL-Signature"
      pattern: "status_code=401"
---

<objective>
Remove the GHL legacy webhook signature bypass. `verify_ghl_legacy_signature` in `api/services/ghl_service.py` unconditionally returns `True` (never verifies the RSA signature), and `api/routers/webhooks.py` falls back to it whenever an `X-WH-Signature` header is present — so any attacker can forge GHL webhook events by simply sending that header. Delete the function and the legacy branch; accept only a valid Ed25519 `X-GHL-Signature`, otherwise return 401. Keep the good Ed25519 path and hardcoded public key intact.

Purpose: Close SEC-03, a remotely exploitable webhook forgery vector (ahead of GHL's July 1, 2026 legacy deprecation).
Output: Ed25519-only GHL webhook verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-critical-security-launch-blockers/05-CONTEXT.md

<interfaces>
From api/services/ghl_service.py (KEEP this function unchanged):
```python
def verify_ghl_ed25519_signature(body: bytes, signature_header: str) -> bool
```
From api/routers/webhooks.py current header params (the x_wh_signature param must be removed):
```python
x_ghl_signature: Optional[str] = Header(default=None, alias="X-GHL-Signature")
x_wh_signature: Optional[str] = Header(default=None, alias="X-WH-Signature")  # REMOVE
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete verify_ghl_legacy_signature from ghl_service.py</name>
  <files>api/services/ghl_service.py</files>
  <read_first>
    - api/services/ghl_service.py (file being edited — contains both functions)
  </read_first>
  <action>
    Delete the entire `verify_ghl_legacy_signature` function (the always-true bypass, including its docstring and body). Keep `verify_ghl_ed25519_signature`, the imports, and `GHL_ED25519_PUBLIC_KEY_PEM` untouched. Update the module docstring at the top of the file so it no longer claims legacy RSA support — replace the "Supports both ... Legacy RSA support must be removed after July 1, 2026." lines with a single line: `Verifies X-GHL-Signature (Ed25519) only. Legacy X-WH-Signature (RSA) support was removed in Phase 5 (SEC-03).`
  </action>
  <verify>
    <automated>! grep -q "verify_ghl_legacy_signature" api/services/ghl_service.py && grep -q "verify_ghl_ed25519_signature" api/services/ghl_service.py && python -m py_compile api/services/ghl_service.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "verify_ghl_legacy_signature" api/services/ghl_service.py` returns NOTHING
    - `grep -n "verify_ghl_ed25519_signature" api/services/ghl_service.py` still returns a match
    - `python -m py_compile api/services/ghl_service.py` exits 0
  </acceptance_criteria>
  <done>ghl_service.py no longer contains the legacy bypass function; only the Ed25519 verifier remains and the module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy branch from the GHL webhook handler</name>
  <files>api/routers/webhooks.py</files>
  <read_first>
    - api/routers/webhooks.py (file being edited — signature branch at top of ghl_webhook)
    - api/services/ghl_service.py (confirm only verify_ghl_ed25519_signature is exported after Task 1)
  </read_first>
  <action>
    In api/routers/webhooks.py:
    1. Change the import `from api.services.ghl_service import verify_ghl_ed25519_signature, verify_ghl_legacy_signature` to import ONLY `verify_ghl_ed25519_signature`.
    2. Remove the `x_wh_signature` Header parameter from the `ghl_webhook` function signature.
    3. Replace the three-branch signature check with a strict Ed25519-only check:
    ```python
    if not x_ghl_signature:
        raise HTTPException(status_code=401, detail="Missing GHL signature header")
    if not verify_ghl_ed25519_signature(body, x_ghl_signature):
        raise HTTPException(status_code=401, detail="Invalid GHL Ed25519 signature")
    ```
    Leave the raw-body read (`body = await request.body()`), payload parsing, event routing, and BackgroundTasks processing unchanged.
  </action>
  <verify>
    <automated>! grep -qi "x_wh_signature\|X-WH-Signature\|verify_ghl_legacy_signature" api/routers/webhooks.py && grep -q "verify_ghl_ed25519_signature" api/routers/webhooks.py && grep -q "status_code=401" api/routers/webhooks.py && python -m py_compile api/routers/webhooks.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -in "x-wh-signature\|x_wh_signature\|verify_ghl_legacy_signature" api/routers/webhooks.py` returns NOTHING
    - `grep -n "verify_ghl_ed25519_signature" api/routers/webhooks.py` returns a match
    - `grep -n "status_code=401" api/routers/webhooks.py` returns matches for both the missing-header and invalid-signature cases
    - `python -m py_compile api/routers/webhooks.py` exits 0
  </acceptance_criteria>
  <done>The GHL webhook accepts only a valid Ed25519 X-GHL-Signature and returns 401 for a missing or invalid signature; no X-WH-Signature handling remains.</done>
</task>

</tasks>

<verification>
- No reference to `verify_ghl_legacy_signature` or `X-WH-Signature` remains anywhere in `api/`.
- The GHL webhook returns 401 on missing/invalid Ed25519 signature.
- Both edited Python modules compile.
</verification>

<success_criteria>
SEC-03 satisfied: the legacy always-true path is gone from the codebase and the GHL webhook enforces Ed25519 verification exclusively.
</success_criteria>

<output>
After completion, create `.planning/phases/05-critical-security-launch-blockers/05-03-SUMMARY.md`
</output>
