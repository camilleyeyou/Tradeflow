---
phase: 05-critical-security-launch-blockers
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_add_leads_callrail_call_id_unique.sql
  - api/services/callrail_service.py
  - api/services/ghl_api.py
  - api/routers/callrail_webhooks.py
  - api/main.py
  - api/.env.example
autonomous: true
requirements: [MISS-01, MISS-02, MISS-03, MISS-04]
must_haves:
  truths:
    - "POST /api/webhooks/callrail rejects (401) any request without a valid CallRail signature/token before processing"
    - "A missed/unanswered call event inserts a calls row and a leads row (source 'direct_call')"
    - "Duplicate deliveries of the same CallRail call id do not create duplicate calls or leads rows"
    - "A missed call triggers the client's GHL text-back workflow within 15 seconds (fired from a BackgroundTask after a fast 200)"
    - "Ingested calls appear in the dashboard call log with recording links (recording_url + outcome populated)"
  artifacts:
    - path: "api/routers/callrail_webhooks.py"
      provides: "CallRail webhook endpoint: verify → 200 fast → background insert + text-back"
      exports: ["router"]
    - path: "api/services/callrail_service.py"
      provides: "CallRail signature verification + payload model + missed-call detection"
      contains: "verify_callrail_signature"
    - path: "api/services/ghl_api.py"
      provides: "Async outbound GHL client to trigger the text-back workflow (httpx.AsyncClient)"
      contains: "trigger_textback"
    - path: "supabase/migrations/005_add_leads_callrail_call_id_unique.sql"
      provides: "Race-safe dedup unique index on leads(callrail_call_id)"
      contains: "unique index"
  key_links:
    - from: "callrail_webhooks.py"
      to: "verify_callrail_signature"
      via: "401 raised before any DB write when signature invalid/missing"
      pattern: "status_code=401"
    - from: "callrail_webhooks.py background task"
      to: "clients / calls / leads (service-role Supabase) + ghl_api.trigger_textback"
      via: "resolve client by callrail_tracking_number, insert calls+leads deduped, then text-back"
      pattern: "trigger_textback"
    - from: "dashboard calls page"
      to: "calls.recording_url / calls.outcome"
      via: "inserted rows carry recording_url and outcome='missed'"
      pattern: "recording_url"
---

<objective>
Build the missing missed-call text-back pipeline advertised on the live marketing site. Add a CallRail webhook to FastAPI that: verifies request authenticity (CallRail v3 signature/token) before any processing and rejects unauthenticated requests (401); on a missed/unanswered call, inserts a deduplicated `calls` row and a `leads` row (source `direct_call`) via the service-role Supabase client; and triggers the client's GHL text-back workflow within 15 seconds. Ingested calls must surface in the existing dashboard call log (which already reads `calls` with recording links).

Purpose: Close MISS-01..04 — capture every inbound call as a lead and auto-text-back so no missed call is ever lost.
Output: New CallRail router + service + async GHL outbound client + dedup index + env docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-critical-security-launch-blockers/05-CONTEXT.md

<interfaces>
<!-- Reuse these existing contracts — do not re-explore the codebase. -->

From api/services/supabase_client.py (service-role client, bypasses RLS):
```python
def get_supabase() -> Client  # supabase-py v2 sync client; .table(name).select/insert/update(...).execute()
```

From supabase/migrations/001_initial_schema.sql (target columns):
```sql
calls(id, client_id NOT NULL, lead_id, callrail_call_id UNIQUE, caller_number,
      tracking_number, duration_seconds, recording_url, transcript, outcome, called_at)
leads(id, client_id NOT NULL, homeowner_name, phone, zip_code, service_type,
      source, status DEFAULT 'new', callrail_call_id, created_at)
clients(id, callrail_tracking_number, ghl_sub_account_id, ...)
```

Dashboard consumer already renders these (do NOT modify unless columns are missing):
- apps/web/src/app/(dashboard)/dashboard/calls/page.tsx  (selects calls.* ordered by called_at desc, RLS-scoped)
- apps/web/src/components/dashboard/calls-table.tsx  (renders caller_number, duration_seconds, outcome, recording_url; getOutcomeVariant handles outcome === 'missed')

Existing GHL webhook pattern to mirror (verify → 200 → BackgroundTasks):
- api/routers/webhooks.py, api/routers/stripe_webhooks.py

Reference for the GHL outbound calls (TS equivalent to port to Python/httpx):
- apps/web/src/lib/ghl.ts (createGHLContact, addContactToWorkflow) — base URL https://services.leadconnectorhq.com, header Version: 2021-07-28
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add race-safe dedup unique index on leads(callrail_call_id)</name>
  <files>supabase/migrations/005_add_leads_callrail_call_id_unique.sql</files>
  <read_first>
    - supabase/migrations/001_initial_schema.sql (leads table + existing calls.callrail_call_id UNIQUE + index style)
    - supabase/migrations/004_enable_rls_client_users.sql (latest migration number/style once 05-02 lands; if absent, 003 is latest)
  </read_first>
  <action>
    Create `supabase/migrations/005_add_leads_callrail_call_id_unique.sql`. `calls.callrail_call_id` is already UNIQUE (migration 001); add the matching partial unique index on `leads` so concurrent CallRail deliveries cannot create duplicate direct_call leads. Content:
    ```sql
    -- 005_add_leads_callrail_call_id_unique.sql
    -- MISS-02: race-safe dedup of missed-call leads on the CallRail call id.
    -- Partial unique index (NULLs allowed) so form-sourced leads without a
    -- callrail_call_id are unaffected. (Broader schema integrity work is Phase 7 HARD-03.)
    create unique index if not exists leads_callrail_call_id_unique
      on public.leads (callrail_call_id)
      where callrail_call_id is not null;
    ```
  </action>
  <verify>
    <automated>test -f supabase/migrations/005_add_leads_callrail_call_id_unique.sql && grep -q "leads_callrail_call_id_unique" supabase/migrations/005_add_leads_callrail_call_id_unique.sql && grep -qi "unique index" supabase/migrations/005_add_leads_callrail_call_id_unique.sql && grep -qi "where callrail_call_id is not null" supabase/migrations/005_add_leads_callrail_call_id_unique.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/005_add_leads_callrail_call_id_unique.sql` exists
    - `grep -i "unique index" ...005....sql` matches
    - `grep -i "where callrail_call_id is not null" ...005....sql` matches (partial index preserves form leads)
  </acceptance_criteria>
  <done>A partial unique index on leads(callrail_call_id) exists in migration 005 so duplicate missed-call leads are rejected at the DB layer.</done>
</task>

<task type="auto">
  <name>Task 2: CallRail signature verification service + async GHL text-back client</name>
  <files>api/services/callrail_service.py, api/services/ghl_api.py, api/.env.example</files>
  <read_first>
    - api/services/ghl_service.py (signature-verification module style; keep verification separate from outbound API)
    - apps/web/src/lib/ghl.ts (createGHLContact + addContactToWorkflow request shapes to port)
    - api/routers/stripe_webhooks.py (env-secret + HTTPException pattern)
    - api/.env.example (env var conventions)
  </read_first>
  <action>
    Create `api/services/callrail_service.py`:
    - `def verify_callrail_signature(body: bytes, signature_header: str | None) -> bool`: return False if `signature_header` is falsy. Read `CALLRAIL_WEBHOOK_SECRET` from env; if unset, log an error and return False (fail closed). Compute `hmac.new(secret.encode(), body, hashlib.sha256)` and compare with `hmac.compare_digest` against the provided signature. Support both hex and base64 digest encodings (compute both, compare_digest against each) since CallRail's exact digest encoding must be confirmed at implementation. IMPLEMENTATION NOTE (leave as a code comment): confirm the exact CallRail v3 signature header name and digest encoding from CallRail docs; default header alias is `Signature`.
    - Pydantic v2 model `CallRailEvent(BaseModel)` with fields matching CallRail call-webhook payloads: `id: str` (map from `call.id`/`resource_id`), `answered: bool | None`, `duration: int | None`, `customer_phone_number: str | None`, `tracking_phone_number: str | None`, `recording: str | None`, `customer_city: str | None = None`, `customer_zip: str | None = None`. Use `model_config = ConfigDict(extra="ignore")`.
    - `def is_missed_call(event: CallRailEvent) -> bool`: return True when the call was not answered (`event.answered is False`) — this is the missed/unanswered condition.

    Create `api/services/ghl_api.py` (outbound GHL API via httpx.AsyncClient — NEVER `requests`):
    - Module constants `GHL_BASE_URL = "https://services.leadconnectorhq.com"`, `GHL_API_VERSION = "2021-07-28"`.
    - `async def _ghl_post(path, token, json_body) -> httpx.Response` using `async with httpx.AsyncClient(timeout=10.0) as client` and headers Authorization Bearer token, Version, Content-Type.
    - `async def create_contact(location_id, phone, token) -> str | None`: POST /contacts/ with {locationId, phone}; return contact id from `contact.id`; on error log with truncated phone (last 4 digits only) and return None.
    - `async def add_to_workflow(contact_id, workflow_id, token) -> bool`: POST /contacts/{contact_id}/workflow/{workflow_id} with {"eventStartTime": <ISO now>}; return True/False.
    - `async def trigger_textback(location_id, phone, token, workflow_id) -> bool`: orchestrate create_contact → add_to_workflow; return False (and log) if either step fails. Leave a clear comment: "Per-client token seam — Phase 6 FIX-01 will pass the client's own stored GHL token; for now the caller supplies the configured token."

    Update `api/.env.example`: add `CALLRAIL_WEBHOOK_SECRET=` (with a comment: CallRail per-webhook signing secret for signature verification), `GHL_PRIVATE_TOKEN=` (GHL sub-account private integration token used for text-back until per-client tokens land in Phase 6 FIX-01), and `GHL_SMS_WORKFLOW_ID=` (pre-configured GHL text-back workflow id).
  </action>
  <verify>
    <automated>python -m py_compile api/services/callrail_service.py api/services/ghl_api.py && grep -q "verify_callrail_signature" api/services/callrail_service.py && grep -q "compare_digest" api/services/callrail_service.py && grep -q "class CallRailEvent" api/services/callrail_service.py && grep -q "is_missed_call" api/services/callrail_service.py && grep -q "trigger_textback" api/services/ghl_api.py && grep -q "httpx.AsyncClient" api/services/ghl_api.py && ! grep -q "import requests" api/services/ghl_api.py && grep -q "CALLRAIL_WEBHOOK_SECRET" api/.env.example && grep -q "GHL_SMS_WORKFLOW_ID" api/.env.example && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile api/services/callrail_service.py api/services/ghl_api.py` exits 0
    - `grep "verify_callrail_signature" api/services/callrail_service.py` and `grep "compare_digest" api/services/callrail_service.py` both match
    - `grep "class CallRailEvent" api/services/callrail_service.py` and `grep "is_missed_call" api/services/callrail_service.py` both match
    - `grep "httpx.AsyncClient" api/services/ghl_api.py` matches; `grep "import requests" api/services/ghl_api.py` returns NOTHING
    - `grep "trigger_textback" api/services/ghl_api.py` matches
    - `grep "CALLRAIL_WEBHOOK_SECRET" api/.env.example` and `grep "GHL_SMS_WORKFLOW_ID" api/.env.example` both match
  </acceptance_criteria>
  <done>Signature verification fails closed without a valid secret+signature; a typed CallRail event model with a missed-call predicate exists; an async httpx-based GHL client can trigger the text-back workflow; env vars are documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CallRail webhook router — verify, insert calls+leads deduped, trigger text-back, register in main</name>
  <files>api/routers/callrail_webhooks.py, api/main.py</files>
  <read_first>
    - api/routers/webhooks.py (verify → 200 → BackgroundTasks structure to mirror)
    - api/routers/stripe_webhooks.py (Header alias + HTTPException usage)
    - api/services/callrail_service.py (verify_callrail_signature, CallRailEvent, is_missed_call — from Task 2)
    - api/services/ghl_api.py (trigger_textback — from Task 2)
    - api/services/supabase_client.py (get_supabase service-role client)
    - api/main.py (router registration + lifespan)
    - apps/web/src/app/(dashboard)/dashboard/calls/page.tsx and apps/web/src/components/dashboard/calls-table.tsx (confirm columns consumed: caller_number, duration_seconds, outcome, recording_url)
  </read_first>
  <action>
    Create `api/routers/callrail_webhooks.py`:
    - `router = APIRouter(prefix="/api", tags=["callrail-webhooks"])`.
    - `@router.post("/webhooks/callrail")` async handler `callrail_webhook(request, background_tasks, signature: Optional[str] = Header(default=None, alias="Signature"))`:
      1. `body = await request.body()` (before any parsing).
      2. `if not verify_callrail_signature(body, signature): raise HTTPException(status_code=401, detail="Invalid CallRail signature")`.
      3. Parse `payload = json.loads(body)`, then `event = CallRailEvent(**payload)` (wrap parse errors → HTTPException 400).
      4. `background_tasks.add_task(process_callrail_event, event)` and `return {"status": "received"}` immediately (fast 200 so the 15s text-back budget is met by the background task).
    - `async def process_callrail_event(event: CallRailEvent) -> None`:
      * `if not is_missed_call(event): log info and return` (only missed/unanswered calls create leads).
      * `db = get_supabase()`.
      * Resolve client: `db.table("clients").select("id, ghl_sub_account_id").eq("callrail_tracking_number", event.tracking_phone_number).execute()`; if no row, log warning (truncated number) and return.
      * Dedup: `db.table("calls").select("id").eq("callrail_call_id", event.id).execute()`; if a row exists, log info and return (idempotent redelivery).
      * Insert calls row with keys: client_id, callrail_call_id=event.id, caller_number=event.customer_phone_number, tracking_number=event.tracking_phone_number, duration_seconds=event.duration, recording_url=event.recording, outcome="missed". (These columns are exactly what the dashboard call log reads — MISS-04.)
      * Insert leads row with: client_id, phone=event.customer_phone_number, zip_code=event.customer_zip, source="direct_call", callrail_call_id=event.id. Wrap the insert in try/except and treat a unique-violation on leads_callrail_call_id_unique as a benign no-op (race dedup).
      * Trigger text-back: read `GHL_PRIVATE_TOKEN` and `GHL_SMS_WORKFLOW_ID` from env; if both present and client has ghl_sub_account_id, `await trigger_textback(location_id=ghl_sub_account_id, phone=event.customer_phone_number, token=token, workflow_id=workflow_id)`. Log (truncated phone) on success/failure. Leave the per-client-token seam comment referencing Phase 6 FIX-01.
      * All logs must truncate PII (log only last 4 digits of phone numbers).
    - Do NOT use `requests`; only the sync supabase client (service role) and the async ghl_api client.

    Update `api/main.py`: `from api.routers import callrail_webhooks` and `app.include_router(callrail_webhooks.router)`.
  </action>
  <verify>
    <automated>python -m py_compile api/routers/callrail_webhooks.py api/main.py && grep -q "/webhooks/callrail" api/routers/callrail_webhooks.py && grep -q "status_code=401" api/routers/callrail_webhooks.py && grep -q "direct_call" api/routers/callrail_webhooks.py && grep -q "recording_url" api/routers/callrail_webhooks.py && grep -q "outcome" api/routers/callrail_webhooks.py && grep -q "trigger_textback" api/routers/callrail_webhooks.py && grep -q "BackgroundTasks\|background_tasks.add_task" api/routers/callrail_webhooks.py && grep -q "callrail_webhooks" api/main.py && ! grep -q "import requests" api/routers/callrail_webhooks.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile api/routers/callrail_webhooks.py api/main.py` exits 0
    - `grep "/webhooks/callrail" api/routers/callrail_webhooks.py` matches; `grep "status_code=401" ...` matches (auth rejection before processing)
    - `grep "direct_call" api/routers/callrail_webhooks.py` matches (leads source)
    - `grep -E "recording_url" api/routers/callrail_webhooks.py` and `grep "outcome" ...` match (MISS-04 columns populated for the dashboard)
    - `grep "trigger_textback" api/routers/callrail_webhooks.py` matches (MISS-03)
    - `grep "background_tasks.add_task\|BackgroundTasks" ...` matches (fast 200 + background processing)
    - `grep "callrail_webhooks" api/main.py` matches (router registered)
    - `grep "import requests" api/routers/callrail_webhooks.py` returns NOTHING
  </acceptance_criteria>
  <done>POST /api/webhooks/callrail verifies the signature (401 on failure), returns 200 fast, and in a background task inserts a deduped calls row and a direct_call leads row then fires the GHL text-back; inserted calls carry recording_url + outcome so they render in the existing dashboard call log. Live end-to-end confirmation (real missed call → text-back) is part of the DPLY deploy checklist.</done>
</task>

</tasks>

<verification>
- Migration 005 adds the partial unique index on leads(callrail_call_id).
- callrail_service.py fails closed on missing secret/signature; ghl_api.py uses httpx.AsyncClient (no requests).
- callrail_webhooks.py enforces 401 before processing, inserts deduped calls+leads, and triggers text-back in a BackgroundTask; router registered in main.py.
- Inserted calls populate recording_url + outcome consumed by the existing dashboard call log.
- All edited Python modules compile.
</verification>

<success_criteria>
MISS-01..04 satisfied in-repo: an authenticated missed-call CallRail webhook creates a deduplicated call + direct_call lead, triggers the GHL text-back from a fast-returning background task, and the ingested call is visible in the dashboard call log with its recording link. (Live 15-second timing and real recording links are confirmed by the DPLY deploy trace.)
</success_criteria>

<output>
After completion, create `.planning/phases/05-critical-security-launch-blockers/05-04-SUMMARY.md`
</output>
