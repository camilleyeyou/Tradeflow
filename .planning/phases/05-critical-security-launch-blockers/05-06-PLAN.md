---
phase: 05-critical-security-launch-blockers
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified: []
autonomous: false
requirements: [DPLY-01, DPLY-02]
must_haves:
  truths:
    - "All Phase 5 migrations (001-005) are applied to the live Supabase project"
    - "Live-generated Supabase types are reconciled with the hand-authored types.ts"
    - "One real lead is traced end-to-end: form → DB → GHL SMS → Resend email"
    - "One real missed call is traced end-to-end: CallRail → calls+lead → GHL text-back within 15s → dashboard call log"
    - "All required env vars are populated across Supabase, Vercel, and Railway"
  artifacts:
    - path: ".planning/phases/05-critical-security-launch-blockers/DEPLOY-CHECKLIST.md"
      provides: "The checklist being executed (authored in 05-05)"
      contains: "Trace A"
  key_links:
    - from: "operator"
      to: "live Supabase / Vercel / Railway / CallRail / GHL"
      via: "executing DEPLOY-CHECKLIST.md steps 1-5"
      pattern: "N/A (human/deploy)"
---

<objective>
Execute the deploy + verification checklist authored in plan 05-05 against live infrastructure. This is the requires-human/deploy step that in-repo work cannot perform: it needs the operator's Supabase/Vercel/Railway/CallRail/GHL credentials and real traffic. It applies migrations 001–005 (including the new SEC-02 client_users RLS and the MISS-02 dedup index), runs the authoritative `supabase gen types typescript` against the live project to reconcile DPLY-01, populates all env vars, and runs the two end-to-end traces that prove the lead pipeline and the missed-call text-back work in production.

Purpose: DPLY-01 (live generation) + DPLY-02 (execution) — the final launch gate for Phase 5.
Output: A verified live deployment with both traces passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-critical-security-launch-blockers/DEPLOY-CHECKLIST.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migrations, regenerate live types, populate env vars</name>
  <read_first>
    - .planning/phases/05-critical-security-launch-blockers/DEPLOY-CHECKLIST.md (sections 1-4)
  </read_first>
  <action>
    REQUIRES-HUMAN/DEPLOY — the operator performs these on live infrastructure (Claude cannot: needs Supabase project credentials, Vercel/Railway dashboard access, and account permissions). Execute DEPLOY-CHECKLIST.md sections 1–4:
    1. Apply migrations in order 001 → 002 → 003 → 004 → 005 to the live Supabase project (`supabase db push` or SQL editor).
    2. Run `supabase gen types typescript --project-id <PROJECT_ID> --schema public > apps/web/src/lib/supabase/types.ts`, diff against the hand-authored file from 05-05, and resolve any drift.
    3. Populate all env vars from the checklist across Supabase, Vercel, and Railway — including CALLRAIL_WEBHOOK_SECRET, GHL_PRIVATE_TOKEN, GHL_SMS_WORKFLOW_ID, ADMIN_EMAILS.
    4. Register the CallRail webhook to `POST https://<railway-host>/api/webhooks/callrail` with the matching signing secret; confirm the GHL webhook targets `/api/webhooks/ghl`.
  </action>
  <what-built>
    Migrations 001–005, schema-accurate types.ts (05-05), and DEPLOY-CHECKLIST.md are ready in-repo. This step applies them to live infrastructure.
  </what-built>
  <how-to-verify>
    - `SELECT tablename, rowsecurity FROM pg_tables WHERE schemaname='public'` shows `rowsecurity = true` for client_users.
    - The `leads_callrail_call_id_unique` index exists on the live DB.
    - `npx tsc --noEmit` still passes after the live type regeneration.
    - Env vars are set in all three platforms and both webhooks are registered.
    Reply with confirmation of each, or describe what failed.
  </how-to-verify>
  <acceptance_criteria>
    - client_users shows rowsecurity = true on the live database
    - leads_callrail_call_id_unique index present on the live database
    - live-generated types.ts committed and `npx tsc --noEmit` passes
    - CALLRAIL_WEBHOOK_SECRET, GHL_PRIVATE_TOKEN, GHL_SMS_WORKFLOW_ID, ADMIN_EMAILS populated on the correct platforms
  </acceptance_criteria>
  <resume-signal>Type "migrations applied + types reconciled + env populated" or describe issues</resume-signal>
  <done>Live schema, types, env vars, and webhook registrations match DEPLOY-CHECKLIST.md sections 1-4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the two end-to-end verification traces</name>
  <read_first>
    - .planning/phases/05-critical-security-launch-blockers/DEPLOY-CHECKLIST.md (section 5)
  </read_first>
  <action>
    REQUIRES-HUMAN/DEPLOY — exercise both pipelines against real traffic per DEPLOY-CHECKLIST.md section 5.
    - Trace A (real lead): submit a landing-page lead form for a test client and follow it form → DB → GHL SMS → Resend email.
    - Trace B (real missed call): call the client's CallRail tracking number and let it ring unanswered; follow it CallRail webhook → calls+lead → GHL text-back → dashboard call log; also replay the webhook with a tampered/absent signature to confirm 401, and redeliver the same call id to confirm no duplicate lead.
  </action>
  <what-built>
    The full lead pipeline (Phases 2–4) plus the new Phase 5 missed-call pipeline are deployed.
  </what-built>
  <how-to-verify>
    Trace A: new row in Supabase `leads`; GHL contact created + SMS workflow enrolled; HVAC owner receives the Resend email.
    Trace B: CallRail webhook returns 200 (and 401 on tampered/absent signature); `calls` row with outcome=missed and recording_url; `leads` row with source=`direct_call` (duplicate delivery creates NO second lead); GHL text-back SMS received within 15 seconds; call appears in the dashboard call log with a working recording link.
    Reply "both traces passed" or describe which hop failed.
  </how-to-verify>
  <acceptance_criteria>
    - Trace A observed at every hop (leads row, GHL contact+workflow, Resend email)
    - Trace B observed at every hop including 401-on-bad-signature and dedup (no duplicate lead)
    - GHL text-back received within 15 seconds of the missed call
  </acceptance_criteria>
  <resume-signal>Type "both traces passed" or describe failures</resume-signal>
  <done>Both end-to-end traces pass in production, including the 401 signature-rejection and dedup checks.</done>
</task>

</tasks>

<verification>
- Migrations 001–005 applied; client_users RLS on; dedup index present.
- Live-generated types reconciled; tsc passes.
- Env vars populated across all three platforms; webhooks registered.
- Trace A (lead) and Trace B (missed call) both pass, including 401-on-bad-signature and dedup checks.
</verification>

<success_criteria>
DPLY-01 live generation and DPLY-02 execution complete: the platform is deployed with generated types, all env vars set, and both end-to-end traces verified in production — the Phase 5 launch gate is cleared.
</success_criteria>

<output>
After completion, create `.planning/phases/05-critical-security-launch-blockers/05-06-SUMMARY.md`
</output>
