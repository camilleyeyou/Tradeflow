---
phase: 06-high-priority-correctness-legal-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/validations/lead.ts
  - api/routers/stripe_webhooks.py
  - apps/web/middleware.ts
  - apps/web/.env.example
  - api/.env.example
  - api/Procfile
  - api/runtime.txt
autonomous: true
requirements: [FIX-03, FIX-04, FIX-05, FIX-06]
must_haves:
  truths:
    - "A phone number typed as '(555) 123-4567' passes lead form validation"
    - "An invalid Stripe signature returns HTTP 400, not 500"
    - "Admin middleware authorizes using ADMIN_EMAILS (comma-separated, case-insensitive) with ADMIN_EMAIL fallback"
    - "The FastAPI service has a pinned Python version and a verified Procfile module path"
  artifacts:
    - path: "apps/web/src/lib/validations/lead.ts"
      provides: "Phone normalization transform before regex"
      contains: "replace(/\\D/g"
    - path: "api/routers/stripe_webhooks.py"
      provides: "Correct Stripe SDK exception class"
      contains: "stripe.SignatureVerificationError"
    - path: "apps/web/middleware.ts"
      provides: "Unified admin gating via ADMIN_EMAILS"
      contains: "ADMIN_EMAILS"
    - path: "api/runtime.txt"
      provides: "Pinned Python version for Railway"
      contains: "3.12"
  key_links:
    - from: "apps/web/middleware.ts"
      to: "ADMIN_EMAILS / ADMIN_EMAIL env"
      via: "inline isAdmin logic"
      pattern: "ADMIN_EMAILS"
---

<objective>
Fix four independent high-priority correctness bugs, each in a distinct file: phone-format validation (FIX-03), the Stripe webhook exception class (FIX-04), unified admin gating (FIX-05), and Railway boot reliability (FIX-06).

Purpose: Lead submission accepts real-world phone formats, invalid Stripe signatures fail with the correct status, admin authorization is consistent across the app, and the backend boots reliably in production.
Output: Edited validation schema, Stripe router, middleware, both `.env.example` files, verified Procfile, and a new Python pin file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-priority-correctness-legal-seo/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FIX-03 phone normalization in lead schema</name>
  <read_first>apps/web/src/lib/validations/lead.ts, apps/web/src/lib/validations/get-started.ts (mirror its phone pattern)</read_first>
  <files>apps/web/src/lib/validations/lead.ts</files>
  <action>
    Replace the `phone` field so it strips non-digits before the regex, mirroring get-started.ts exactly:
    `phone: z.string().transform((v) => v.replace(/\D/g, '')).pipe(z.string().regex(/^\+?1?\d{10}$/, 'Enter a valid 10-digit US phone number')),`
    Keep `LeadFormValues` usable: because `.transform()` changes the inferred input/output shape, change the exported type to `export type LeadFormValues = z.input<typeof leadSchema>` so the react-hook-form input type (raw string) still matches lead-form.tsx defaultValues. Do NOT change any other field.
  </action>
  <verify>
    <automated>grep -q "replace(/\\\\D/g" apps/web/src/lib/validations/lead.ts && grep -q "z.input<typeof leadSchema>" apps/web/src/lib/validations/lead.ts && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>lead.ts strips non-digits before regex; "(555) 123-4567" would parse to "5551234567"; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: FIX-04 correct Stripe exception + malformed-body handling</name>
  <read_first>api/routers/stripe_webhooks.py, api/requirements.txt (confirms stripe==14.4.1)</read_first>
  <files>api/routers/stripe_webhooks.py</files>
  <action>
    Change the except clause `except stripe.errors.SignatureVerificationError:` to `except stripe.SignatureVerificationError:` (correct for stripe 14.4.1). Leave the existing `except ValueError:` → 400 branch (it already covers malformed payloads that stripe.Webhook.construct_event raises). Both branches must raise HTTPException status_code=400. Do not change the signature-missing (400) or secret-missing (500) branches.
  </action>
  <verify>
    <automated>grep -q "except stripe.SignatureVerificationError:" api/routers/stripe_webhooks.py && ! grep -q "stripe.errors.SignatureVerificationError" api/routers/stripe_webhooks.py && python -m py_compile api/routers/stripe_webhooks.py</automated>
  </verify>
  <done>Exception references `stripe.SignatureVerificationError`; invalid signature raises 400; file compiles.</done>
</task>

<task type="auto">
  <name>Task 3: FIX-05 unified admin gating in middleware + ADMIN_EMAILS docs</name>
  <read_first>apps/web/middleware.ts, apps/web/src/lib/admin.ts (isAdmin logic to mirror), apps/web/.env.example, api/.env.example</read_first>
  <files>apps/web/middleware.ts, apps/web/.env.example, api/.env.example</files>
  <action>
    In middleware.ts replace the line `if (isAdmin && user?.email !== process.env.ADMIN_EMAIL)` gate with the same logic as src/lib/admin.ts, inlined (middleware runs on the edge, so avoid importing modules that may pull server-only deps — inline is safest). Add a local helper inside the file:
    `function emailIsAdmin(email: string | null | undefined): boolean { if (!email) return false; const raw = process.env.ADMIN_EMAILS ?? process.env.ADMIN_EMAIL ?? ''; return raw.split(',').map(e => e.trim().toLowerCase()).filter(Boolean).includes(email.toLowerCase()); }`
    Rename the existing local `const isAdmin = path.startsWith('/admin')` to `const isAdminPath = path.startsWith('/admin')` to avoid shadowing, update its two usages, and change the admin gate to `if (isAdminPath && !emailIsAdmin(user?.email))`.
    Add `ADMIN_EMAILS=` (with a comment: `# Comma-separated admin emails; ADMIN_EMAIL below kept as fallback`) directly above the existing `ADMIN_EMAIL=` line in BOTH apps/web/.env.example and api/.env.example.
  </action>
  <verify>
    <automated>grep -q "ADMIN_EMAILS" apps/web/middleware.ts && grep -q "emailIsAdmin" apps/web/middleware.ts && grep -q "^ADMIN_EMAILS=" apps/web/.env.example && grep -q "^ADMIN_EMAILS=" api/.env.example && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Middleware uses ADMIN_EMAILS (comma-split, case-insensitive) with ADMIN_EMAIL fallback; both .env.example files document ADMIN_EMAILS; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 4: FIX-06 pin Python + verify Procfile module path</name>
  <read_first>api/Procfile, api/main.py (uses `from api.routers import ...` dotted imports → service root must be repo root), api/requirements.txt</read_first>
  <files>api/runtime.txt, api/Procfile</files>
  <action>
    Create api/runtime.txt containing exactly `python-3.12.x` on one line (use `python-3.12.7`) — Railway/Nixpacks reads runtime.txt to pin the interpreter, matching CLAUDE.md's Python 3.12.
    In api/Procfile, keep `web: uvicorn api.main:app --host 0.0.0.0 --port $PORT` but add a comment line ABOVE it documenting the deploy invariant: `# Railway service root MUST be the repo root (not api/) so that the 'api' package resolves for 'api.main:app' and 'from api.routers import ...'.` Do not change the uvicorn command — the dotted `api.main:app` path is correct only when run from repo root, which the comment now makes explicit.
  </action>
  <verify>
    <automated>test -f api/runtime.txt && grep -q "3.12" api/runtime.txt && grep -q "uvicorn api.main:app" api/Procfile && grep -q "repo root" api/Procfile</automated>
  </verify>
  <done>api/runtime.txt pins Python 3.12; Procfile documents the repo-root service-root requirement; uvicorn module path unchanged and verified against main.py's dotted imports.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes.
- `python -m py_compile api/routers/stripe_webhooks.py` passes.
- No landing-page client JS changed (form validation only) — page stays <2s.
</verification>

<success_criteria>
FIX-03/04/05/06 all satisfied: phone normalization present, Stripe exception corrected to 400, middleware unified on ADMIN_EMAILS, Python pinned + Procfile verified.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-priority-correctness-legal-seo/06-01-SUMMARY.md`
</output>
