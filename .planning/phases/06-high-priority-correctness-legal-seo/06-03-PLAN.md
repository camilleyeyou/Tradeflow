---
phase: 06-high-priority-correctness-legal-seo
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - apps/web/src/components/lead-form.tsx
  - apps/web/src/app/(marketing)/privacy/page.tsx
  - apps/web/src/app/(marketing)/terms/page.tsx
  - apps/web/src/app/(marketing)/page.tsx
  - apps/web/src/app/(marketing)/get-started/page.tsx
  - supabase/migrations/007_add_reviews_to_clients.sql
  - apps/web/src/lib/supabase/types.ts
  - apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx
autonomous: true
requirements: [LEGL-01, LEGL-02, LEGL-03]
must_haves:
  truths:
    - "The lead form shows SMS/call consent language before submission"
    - "Privacy Policy and Terms of Service pages are live and linked from the marketing and landing-page footers"
    - "A landing page's review rating and count come from that client's own data and are hidden when absent — no hardcoded 4.9 / 100+"
  artifacts:
    - path: "apps/web/src/app/(marketing)/privacy/page.tsx"
      provides: "Privacy Policy page"
      min_lines: 20
    - path: "apps/web/src/app/(marketing)/terms/page.tsx"
      provides: "Terms of Service page"
      min_lines: 20
    - path: "supabase/migrations/007_add_reviews_to_clients.sql"
      provides: "review_rating + review_count columns"
      contains: "review_rating"
  key_links:
    - from: "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx"
      to: "clients.review_rating / clients.review_count"
      via: "conditional render of trust block"
      pattern: "review_rating"
    - from: "apps/web/src/app/(marketing)/page.tsx"
      to: "/privacy and /terms"
      via: "footer links"
      pattern: "/privacy"
---

<objective>
Meet baseline SMS/legal compliance: add SMS/call consent copy at the point of lead capture (LEGL-01), create live Privacy Policy and Terms pages linked from every public footer (LEGL-02), and replace the hardcoded "4.9 / 100+ reviews" claim with per-client data that hides when absent (LEGL-03).

Purpose: The platform enrolls phone numbers in automated SMS, so it must disclose consent and publish legal pages; review claims must be truthful per client.
Output: Consent text in the lead form, two static legal pages, footer links across marketing + landing, migration 007 with review columns, updated types, and a data-driven landing trust block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-priority-correctness-legal-seo/06-CONTEXT.md

<interfaces>
Landing page.tsx currently hardcodes "4.9 from 100+ reviews" (hero trust strip, ~line 215) and a "4.9" + "100+ reviews" card (~line 290-296). It selects clients columns at ~line 132: `.select('id, business_name, phone, city, service_area_zips, slug')`.
Marketing footer: apps/web/src/app/(marketing)/page.tsx ~line 486-500 (uses `Link` and `GOLD`). Get-started footer: apps/web/src/app/(marketing)/get-started/page.tsx ~line 295-315. Landing page.tsx currently has NO footer.
lead-form.tsx receives props { clientId, serviceType } only — it does NOT currently receive business name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LEGL-01 consent copy in lead form</name>
  <read_first>apps/web/src/components/lead-form.tsx (props interface + submit button area, roughly lines 216-259), apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx (where LeadForm is rendered, roughly line 251)</read_first>
  <files>apps/web/src/components/lead-form.tsx, apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx</files>
  <action>
    Add an optional `businessName?: string` prop to LeadFormProps and destructure it in the component signature. In the landing page pass `businessName={client.business_name as string}` to the rendered LeadForm.
    Directly below the submit button in lead-form.tsx (below the existing "No spam" line), add a static consent paragraph:
    `<p className="mt-2 text-center text-xs text-slate-500">By submitting, you agree to receive calls and texts (including automated) from {businessName ?? 'this business'} about your request. Msg &amp; data rates may apply. Reply STOP to opt out.</p>`
    This is static text with no new client state, so landing pages stay under 2s.
  </action>
  <verify>
    <automated>grep -q "Reply STOP to opt out" apps/web/src/components/lead-form.tsx && grep -q "businessName" apps/web/src/components/lead-form.tsx && grep -q "business_name" "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx" && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Lead form renders consent language naming the business (or a safe default); tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: LEGL-02 privacy + terms pages and footer links</name>
  <read_first>apps/web/src/app/(marketing)/page.tsx (footer, roughly lines 486-500; note GOLD const + Link import at top), apps/web/src/app/(marketing)/get-started/page.tsx (footer, roughly lines 295-315), apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx (no footer yet)</read_first>
  <files>apps/web/src/app/(marketing)/privacy/page.tsx, apps/web/src/app/(marketing)/terms/page.tsx, apps/web/src/app/(marketing)/page.tsx, apps/web/src/app/(marketing)/get-started/page.tsx, apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx</files>
  <action>
    Create apps/web/src/app/(marketing)/privacy/page.tsx and apps/web/src/app/(marketing)/terms/page.tsx as static Server Components (default export, no 'use client'). Each: export a `metadata` object with a title ("Privacy Policy | Tradeflow" / "Terms of Service | Tradeflow"), and render a readable single-column article (max-w-3xl mx-auto, prose-like Tailwind) with baseline HVAC lead-gen SaaS content: for Privacy — what data is collected (name, phone, service, zip), how it is used (contacting the homeowner, SMS/call follow-up), SMS consent + STOP/HELP opt-out, third-party processors (GoHighLevel, CallRail, Stripe, Resend, Supabase), and a contact email hello@tradeflow-technologies.com; for Terms — service description, acceptable use, no-guarantee/limitation-of-liability, billing/retainer note, governing law (Illinois), and the same contact email. Match the dark marketing aesthetic minimally (bg-black text-white or a simple light article is acceptable — keep it static, no heavy JS). Include a `Last updated: 2026-07-06` line.
    Add footer links to `/privacy` and `/terms` in BOTH marketing footers (page.tsx and get-started/page.tsx) using the existing `Link` component next to the existing footer links.
    Add a minimal footer to the landing page (apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx) after the closing `</section>` of the trust-details block and before `</main>`: a small centered `<footer>` with `<a href="/privacy">Privacy</a>` and `<a href="/terms">Terms</a>` (plain anchors are fine here since the landing route group is static) plus a copyright line. Keep it lightweight.
  </action>
  <verify>
    <automated>test -f "apps/web/src/app/(marketing)/privacy/page.tsx" && test -f "apps/web/src/app/(marketing)/terms/page.tsx" && grep -q "/privacy" "apps/web/src/app/(marketing)/page.tsx" && grep -q "/terms" "apps/web/src/app/(marketing)/page.tsx" && grep -q "/privacy" "apps/web/src/app/(marketing)/get-started/page.tsx" && grep -q "/privacy" "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx" && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>/privacy and /terms exist and are linked from the marketing home footer, the get-started footer, and the landing-page footer; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 007 + types for per-client reviews</name>
  <read_first>supabase/migrations/006_add_ghl_token_to_clients.sql (created in plan 06-02; confirm 006 is taken so this is 007), apps/web/src/lib/supabase/types.ts (clients Row/Insert/Update — already includes ghl_private_token_encrypted from 06-02)</read_first>
  <files>supabase/migrations/007_add_reviews_to_clients.sql, apps/web/src/lib/supabase/types.ts</files>
  <action>
    Create supabase/migrations/007_add_reviews_to_clients.sql:
    `-- 007_add_reviews_to_clients.sql`
    `-- Per-client review rating/count for landing-page trust block (LEGL-03). Null = hidden.`
    `ALTER TABLE public.clients ADD COLUMN IF NOT EXISTS review_rating numeric(2,1);`
    `ALTER TABLE public.clients ADD COLUMN IF NOT EXISTS review_count integer;`
    In types.ts add `review_rating: number | null` and `review_count: number | null` to the clients `Row`, and the `?:`-optional nullable forms to `Insert` and `Update`.
  </action>
  <verify>
    <automated>grep -q "review_rating" supabase/migrations/007_add_reviews_to_clients.sql && grep -q "review_count" supabase/migrations/007_add_reviews_to_clients.sql && grep -c "review_rating" apps/web/src/lib/supabase/types.ts | grep -qx 3 && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Migration 007 adds review_rating + review_count; types.ts carries both across Row/Insert/Update; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 4: LEGL-03 data-driven landing trust block</name>
  <read_first>apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx (client select ~line 132; hero trust strip ~line 212-219; reviews card ~line 285-296)</read_first>
  <files>apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx</files>
  <action>
    Extend the clients select to fetch the new columns: change `.select('id, business_name, phone, city, service_area_zips, slug')` to `.select('id, business_name, phone, city, service_area_zips, slug, review_rating, review_count')`.
    Compute `const hasReviews = client.review_rating != null && client.review_count != null`.
    HERO trust strip: replace the hardcoded `<span className="ml-1.5 text-white/80">4.9 from 100+ reviews</span>` (and its five StarIcon siblings) so the whole star+reviews `<span>` renders only when `hasReviews`, showing `{client.review_rating} from {client.review_count}+ reviews`. When not hasReviews, render neither the stars nor the text (keep "Licensed & insured" as-is).
    TRUST DETAILS reviews card (the "4.9" / "100+ reviews" card): render this entire third card only when `hasReviews`, using `{client.review_rating}` and `{client.review_count}+ reviews`. When absent, omit the card (the grid may show two cards).
    Remove every hardcoded "4.9" and "100+" literal from the file.
  </action>
  <verify>
    <automated>grep -q "review_rating" "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx" && grep -q "hasReviews" "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx" && ! grep -q "4.9" "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx" && ! grep -q "100+ reviews" "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx" && cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Landing page reads review_rating/review_count, renders the trust stars and review card only when both are present, and contains no hardcoded 4.9/100+; tsc passes.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes.
- Migration 007 is the only new migration in this plan (006 belongs to plan 06-02).
- Legal pages and consent copy are static (no heavy client JS) — landing pages stay <2s.
</verification>

<success_criteria>
LEGL-01 (consent copy), LEGL-02 (privacy/terms pages + footer links on marketing and landing), and LEGL-03 (per-client, hide-when-absent reviews) all satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-priority-correctness-legal-seo/06-03-SUMMARY.md`
</output>
