---
phase: 06-high-priority-correctness-legal-seo
plan: 06
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
files_modified:
  - apps/web/package.json
  - apps/web/package-lock.json
autonomous: true
requirements: [DEP-01]
must_haves:
  truths:
    - "npm audit (production deps) shows no unresolved high-severity findings"
    - "Next.js is on a patched 15.5.x"
    - "The full app still typechecks and builds after the dependency updates"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Patched dependency versions"
      contains: "\"next\""
  key_links:
    - from: "apps/web/package.json"
      to: "apps/web/package-lock.json"
      via: "npm install lockfile sync"
      pattern: "next"
---

<objective>
Patch known-vulnerable production dependencies (DEP-01): resolve npm audit high-severity advisories (currently including `ws` and the `resend`→`svix`→`ws`/`uuid` chain) and bump Next.js to a patched 15.5.x, then confirm no regressions via typecheck and build.

Purpose: The app must not ship with high-severity advisories in production dependencies before onboarding paying clients.
Output: Updated package.json + package-lock.json with a clean `npm audit --omit=dev` high-severity report and a passing build.

Runs last so build verification covers every change made in plans 06-01 through 06-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-priority-correctness-legal-seo/06-CONTEXT.md

<interfaces>
Current: apps/web next@15.5.14 installed. `npm audit --omit=dev` reports high-severity items including `ws` (GHSA-58qx-3vcg-4xpx, GHSA-96hv-2xvq-fx4p) reachable via `resend` → `svix` → `ws`, and a vulnerable `uuid`. `npm audit fix` reports a fix is available for `ws` without --force.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit baseline + apply non-breaking fixes</name>
  <read_first>apps/web/package.json (dependency versions), CLAUDE.md (locked stack: resend 6.9.x, next 15.5.x)</read_first>
  <files>apps/web/package.json, apps/web/package-lock.json</files>
  <action>
    From apps/web: run `npm audit --omit=dev` and capture the high-severity list into the SUMMARY. Then run `npm audit fix` (NON --force) to resolve advisories that do not require breaking changes (this should clear the `ws`/`svix` transitive chain and `uuid`). Do NOT run `npm audit fix --force` (it can downgrade/upgrade across majors and break the locked stack). Re-run `npm audit --omit=dev` and record the delta.
  </action>
  <verify>
    <automated>cd apps/web && npm audit fix && npm audit --omit=dev --audit-level=high; test $? -eq 0</automated>
  </verify>
  <done>`npm audit --omit=dev --audit-level=high` exits 0 (no high-severity production findings); the ws/svix/uuid chain is resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure patched Next.js 15.5.x + regression build</name>
  <read_first>apps/web/package.json (current next 15.5.14)</read_first>
  <files>apps/web/package.json, apps/web/package-lock.json</files>
  <action>
    Confirm Next.js resolves to the latest patched 15.5.x. Run `npm install next@^15.5` (stays within the locked 15.5 minor per CLAUDE.md — do not jump to 15.6/16). If any high-severity Next.js advisory remains after Task 1, this bump clears it. Then run the full regression gate: `npx tsc --noEmit` and `npm run build`. If the build surfaces an issue introduced by a dependency bump, prefer a targeted version pin over reverting the security fix; document any such pin in the SUMMARY.
  </action>
  <verify>
    <automated>cd apps/web && node -e "const v=require('next/package.json').version; if(!/^15\.5\./.test(v)){console.error('next not 15.5.x: '+v);process.exit(1)}" && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Next.js is on a patched 15.5.x; `npx tsc --noEmit` and `npm run build` both succeed with all Phase 6 changes in place.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npm audit --omit=dev --audit-level=high` exits 0.
- `cd apps/web && npx tsc --noEmit` passes.
- `cd apps/web && npm run build` passes (covers plans 06-01..06-05).
</verification>

<success_criteria>
DEP-01 satisfied: no unresolved high-severity production advisories, Next.js on patched 15.5.x, and a clean build with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-priority-correctness-legal-seo/06-06-SUMMARY.md`
</output>
