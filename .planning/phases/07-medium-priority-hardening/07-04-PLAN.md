---
phase: 07-medium-priority-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/lib/env.ts
  - apps/web/src/instrumentation.ts
autonomous: true
requirements: [HARD-07, HARD-09]
must_haves:
  truths:
    - "No render-blocking third-party font <link> (api.fontshare.com) remains; typography is served by self-hosted next/font"
    - "The Next.js server validates required env vars at boot (instrumentation register hook) and throws a clear error listing what is missing"
    - "The production build still succeeds"
  artifacts:
    - path: "apps/web/src/lib/env.ts"
      provides: "validateEnv() — zod-validated required env check"
      exports: ["validateEnv"]
    - path: "apps/web/src/instrumentation.ts"
      provides: "register() that runs validateEnv() on the Node.js runtime at boot"
      exports: ["register"]
  key_links:
    - from: "apps/web/src/instrumentation.ts"
      to: "apps/web/src/lib/env.ts"
      via: "register() imports and calls validateEnv() when NEXT_RUNTIME === 'nodejs'"
      pattern: "validateEnv"
---

<objective>
Remove render-blocking third-party font requests from the app shell (HARD-07) and add fail-fast environment validation for the Next.js app at server boot (HARD-09, Next.js side). Touches only layout.tsx and two new files — no overlap with the Next.js correctness plan.

Purpose: Landing pages must not depend on runtime font fetches from a third party; the server must refuse to boot silently when misconfigured.
Output: layout.tsx with Fontshare links removed, apps/web/src/lib/env.ts, apps/web/src/instrumentation.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-medium-priority-hardening/07-CONTEXT.md

<interfaces>
layout.tsx currently: next/font/google Poppins (--font-sans) + Geist_Mono (--font-geist-mono) are already self-hosted at build. The <head> also injects three render-blocking Fontshare stylesheets + a preconnect to https://api.fontshare.com — these are the only third-party font requests.
globals.css maps `--font-sans` and `--font-heading` both to `var(--font-sans)` (Poppins). No CSS font-family rule references General Sans / Gambetta.
Marketing/dashboard components use inline `style={{ fontFamily: "'General Sans', system-ui, sans-serif" }}` and `"'Gambetta', Georgia, serif"` — all have system fallbacks, so removing the Fontshare links degrades gracefully to system-ui / Georgia (the accepted typography tradeoff per CONTEXT; do NOT attempt to fetch external font binaries).
zod is already a project dependency. NEXT_RUNTIME is set by Next.js ('nodejs' | 'edge'). instrumentation.ts is stable in Next.js 15 (no config flag needed).
Required Next.js server env vars: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove render-blocking Fontshare links (HARD-07)</name>
  <files>apps/web/src/app/layout.tsx</files>
  <read_first>
    - apps/web/src/app/layout.tsx
    - apps/web/src/app/globals.css (confirm no CSS rule depends on General Sans/Gambetta)
  </read_first>
  <action>
    In apps/web/src/app/layout.tsx delete the entire `<head>...</head>` block that contains the preconnect and the two api.fontshare.com stylesheet <link> tags (lines wiring General Sans + Gambetta). Keep the next/font Poppins + Geist_Mono setup and the `<body className={...}>`. The app now relies on self-hosted next/font (Poppins via --font-sans/--font-heading) with system fallbacks for the inline General Sans/Gambetta references — no third-party font request remains.
    Do not add any new external <link> or @import for fonts.
  </action>
  <acceptance_criteria>
    - `grep -c "fontshare" apps/web/src/app/layout.tsx` returns 0
    - `grep -c "rel=\"stylesheet\"" apps/web/src/app/layout.tsx` returns 0
    - `grep -q "next/font/google" apps/web/src/app/layout.tsx` (self-hosted fonts retained)
    - From apps/web, `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c fontshare apps/web/src/app/layout.tsx)" -eq 0 ] && grep -q "next/font/google" apps/web/src/app/layout.tsx && echo OK</automated>
  </verify>
  <done>No api.fontshare.com preconnect or stylesheet <link> remains in layout.tsx; next/font self-hosting retained; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fail-fast env validation at boot (HARD-09, Next.js side)</name>
  <files>apps/web/src/lib/env.ts, apps/web/src/instrumentation.ts</files>
  <read_first>
    - apps/web/src/lib/supabase/server.ts (env var names)
    - apps/web/src/lib/supabase/admin.ts (env var names)
  </read_first>
  <action>
    1. Create apps/web/src/lib/env.ts:
       ```ts
       import { z } from 'zod'

       const serverEnvSchema = z.object({
         NEXT_PUBLIC_SUPABASE_URL: z.string().url(),
         NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().min(1),
         SUPABASE_SERVICE_ROLE_KEY: z.string().min(1),
         RESEND_API_KEY: z.string().min(1),
       })

       export function validateEnv(): void {
         const parsed = serverEnvSchema.safeParse(process.env)
         if (!parsed.success) {
           const missing = parsed.error.issues.map((i) => i.path.join('.')).join(', ')
           throw new Error(
             `[env] Missing or invalid required environment variables: ${missing}`
           )
         }
       }
       ```
       (Do NOT parse at module top-level — only inside validateEnv(), so the build does not crash.)
    2. Create apps/web/src/instrumentation.ts:
       ```ts
       export async function register() {
         if (process.env.NEXT_RUNTIME === 'nodejs') {
           const { validateEnv } = await import('@/lib/env')
           validateEnv()
         }
       }
       ```
  </action>
  <acceptance_criteria>
    - `grep -q "export function validateEnv" apps/web/src/lib/env.ts` and `grep -q "from 'zod'" apps/web/src/lib/env.ts`
    - `grep -q "export async function register" apps/web/src/instrumentation.ts`
    - `grep -q "validateEnv" apps/web/src/instrumentation.ts` and `grep -q "NEXT_RUNTIME === 'nodejs'" apps/web/src/instrumentation.ts`
    - From apps/web, `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export function validateEnv" apps/web/src/lib/env.ts && grep -q "export async function register" apps/web/src/instrumentation.ts && grep -q "NEXT_RUNTIME === 'nodejs'" apps/web/src/instrumentation.ts && echo OK</automated>
  </verify>
  <done>env.ts exports zod-validated validateEnv(); instrumentation.ts register() calls it on the Node.js runtime; tsc passes.</done>
</task>

</tasks>

<verification>
- From apps/web: `npm run build` passes (proves HARD-07 removal and instrumentation register wiring compile and run at boot without breaking the build).
- No api.fontshare.com reference remains anywhere in layout.tsx.
</verification>

<success_criteria>
HARD-07 and HARD-09 (Next.js) satisfied: no render-blocking third-party font requests, and the Next.js server validates required env vars at boot with a clear failure message.
</success_criteria>

<output>
After completion, create `.planning/phases/07-medium-priority-hardening/07-04-SUMMARY.md`
</output>
