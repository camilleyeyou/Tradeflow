---
phase: 08-low-priority-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "CLAUDE (1).md" (deleted)
  - apps/web/.git (deleted, directory)
  - apps/web/public/next.svg (deleted)
  - apps/web/public/vercel.svg (deleted)
  - apps/web/public/file.svg (deleted)
  - apps/web/public/globe.svg (deleted)
  - apps/web/public/window.svg (deleted)
  - .gitignore
  - apps/web/src/app/not-found.tsx
  - apps/web/src/app/global-error.tsx
  - apps/web/src/components/lead-form.tsx
  - "apps/web/src/app/(admin)/admin/clients/[id]/page.tsx"
  - apps/web/README.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03, HYG-04]

must_haves:
  truths:
    - "A contributor cloning the repo finds no stale duplicate CLAUDE file, no unused create-next-app SVGs, no nested .git repo under apps/web, and the untracked brand-binaries folder is gitignored"
    - "An ad-click visitor who hits a bad/unknown client slug sees a styled on-brand 404 page instead of the default Next.js error screen"
    - "An unhandled render error anywhere in the app is caught by a global error boundary with a friendly message and a retry action, instead of a blank white screen"
    - "The lead-form success message no longer claims a specific hardcoded area code"
    - "The admin client-detail 'All Clients' back-link is readable against the dark background on hover"
    - "The web README and ROADMAP progress table describe only features that have actually shipped, with no internal contradictions"
  artifacts:
    - path: apps/web/src/app/not-found.tsx
      provides: "Styled 404 page (default export, on-brand black+gold, link home)"
    - path: apps/web/src/app/global-error.tsx
      provides: "Global error boundary (own html/body, retry button, 'use client')"
    - path: .gitignore
      provides: "TRADEFLOW-DESIGN-A-ALL-FILES/ ignore rule"
  key_links:
    - from: apps/web/src/app/not-found.tsx
      to: "app root layout"
      via: "Next.js App Router convention (auto-wired, no import needed)"
      pattern: "export default function NotFound"
    - from: apps/web/src/app/global-error.tsx
      to: "app root layout"
      via: "Next.js App Router convention (auto-wired, replaces root layout on uncaught error)"
      pattern: "export default function GlobalError"
---

<objective>
Close out Phase 8 (Low-Priority Hygiene): remove stale repo cruft, add a styled not-found page and a global error boundary, fix two pieces of hardcoded/low-contrast copy, and correct the web README + ROADMAP progress table so both accurately describe shipped features.

Purpose: Ad-click visitors and future contributors should not hit stale files, broken pages, or inaccurate docs. This is the last phase before Phase 9 features work begins.
Output: Clean git tree (no nested `.git`, no duplicate `CLAUDE (1).md`, no unused SVGs, brand-binaries dir gitignored), `apps/web/src/app/not-found.tsx` + `global-error.tsx`, corrected copy in two components, an accurate `apps/web/README.md`, and a self-consistent `.planning/ROADMAP.md` progress table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-low-priority-hygiene/08-CONTEXT.md

<interfaces>
<!-- Existing (admin) error boundary — use as the visual/structural reference for the new
     root-level not-found.tsx and global-error.tsx. Do not import from it; it is scoped to
     the (admin) route group and stays in place. -->

From apps/web/src/app/(admin)/error.tsx (pattern to mirror, NOT to modify):
```tsx
'use client'
import { useEffect } from 'react'
import Link from 'next/link'

export default function AdminError({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) {
  useEffect(() => { console.error('[admin]', error) }, [error])
  return (
    <div className="min-h-screen bg-black text-white px-6 py-16">
      {/* ...on-brand black background, rgba(239,68,68,...) error card, Retry button calling reset() ... */}
    </div>
  )
}
```

Brand tokens used throughout the marketing/landing pages (apps/web/src/app/(marketing)/page.tsx):
- Background: `bg-black` / `text-white`
- Gold accent: `const GOLD = '#D4AF37'`, used inline as `style={{ color: GOLD }}` or `style={{ background: GOLD }}`
- Wordmark: `Trade<span style={{ color: GOLD }}>flow</span>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale repo cruft (HYG-01)</name>
  <files>CLAUDE (1).md, apps/web/.git, apps/web/public/next.svg, apps/web/public/vercel.svg, apps/web/public/file.svg, apps/web/public/globe.svg, apps/web/public/window.svg, .gitignore</files>
  <read_first>
    - Confirmed via `grep -rn "next.svg\|vercel.svg\|file.svg\|globe.svg\|window.svg" apps/web/src` returning zero matches — these 5 create-next-app default SVGs are unreferenced.
    - Confirmed `apps/web/.git` exists as a real nested git directory (has HEAD/config/objects/refs) — a stray nested repo, not a submodule (verified via `git ls-files | grep "apps/web/"` in the ROOT repo showing apps/web files are tracked directly by the root repo).
    - Confirmed `CLAUDE (1).md` exists at repo root (23KB stale duplicate of CLAUDE.md).
    - Confirmed `TRADEFLOW-DESIGN-A-ALL-FILES/` exists at repo root, is untracked (`git status --short` shows `?? TRADEFLOW-DESIGN-A-ALL-FILES/`), and is not yet in `.gitignore`.
  </read_first>
  <action>
    Run exactly these deletions and edit, from the repo root:
    1. `rm -f "CLAUDE (1).md"`
    2. `rm -rf apps/web/.git`
    3. `rm -f apps/web/public/next.svg apps/web/public/vercel.svg apps/web/public/file.svg apps/web/public/globe.svg apps/web/public/window.svg`
    4. Append a new line to `.gitignore` (after the existing `tradeflow-brand-pack-*.zip` line): `TRADEFLOW-DESIGN-A-ALL-FILES/`

    Do NOT touch `apps/web/public/logo.svg`, `logo-icon.svg`, `logo-light.svg`, or the `apps/web/public/images/` directory — those are in-use brand assets.
    Do NOT delete the `TRADEFLOW-DESIGN-A-ALL-FILES/` directory itself — only gitignore it (it is the user's local asset export, not repo content).
  </action>
  <verify>
    <automated>test ! -e "CLAUDE (1).md" && test ! -d apps/web/.git && test ! -f apps/web/public/next.svg && test ! -f apps/web/public/vercel.svg && test ! -f apps/web/public/file.svg && test ! -f apps/web/public/globe.svg && test ! -f apps/web/public/window.svg && grep -qx "TRADEFLOW-DESIGN-A-ALL-FILES/" .gitignore && echo PASS</automated>
  </verify>
  <done>All 6 stale paths no longer exist on disk; `.gitignore` contains the exact line `TRADEFLOW-DESIGN-A-ALL-FILES/`; `apps/web/public/logo*.svg` and `apps/web/public/images/` are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add styled not-found and global-error pages (HYG-02)</name>
  <files>apps/web/src/app/not-found.tsx, apps/web/src/app/global-error.tsx</files>
  <action>
    Create `apps/web/src/app/not-found.tsx` — a Server Component (no `'use client'`), on-brand black+gold, default export:
    - `min-h-screen bg-black text-white` container, centered content
    - Large heading "404" in gold (`style={{ color: '#D4AF37' }}`), sub-heading "Page not found", one line of body copy (e.g. "The page you're looking for doesn't exist or may have moved.")
    - A `<Link href="/">` styled as a gold button (mirror the CTA button style from `(marketing)/page.tsx`: `style={{ background: '#D4AF37', color: '#000' }}` with rounded corners) reading "Back to home"
    - This file is picked up automatically by Next.js App Router for any unmatched route (including bad `/[clientSlug]/[service]` combos) and when `notFound()` is called — no wiring/import needed.

    Create `apps/web/src/app/global-error.tsx` — MUST be a Client Component (`'use client'` at the top) and MUST render its own `<html>` and `<body>` tags because it replaces the root layout when triggered (per Next.js App Router convention for `global-error.tsx`):
    - `export default function GlobalError({ error, reset }: { error: Error & { digest?: string }; reset: () => void })`
    - `useEffect` that calls `console.error('[global-error]', error)` (mirror the pattern in `apps/web/src/app/(admin)/error.tsx`)
    - Render `<html lang="en"><body className="min-h-screen bg-black text-white ...">` with a friendly heading ("Something went wrong"), one line of body copy, and a `<button onClick={reset}>` styled as a gold button reading "Try again"
    - Do not import any Server Component or data-fetching code here — this file must render standalone even if the root layout itself failed.
  </action>
  <verify>
    <automated>cd apps/web && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>Both files exist, export a default component, `global-error.tsx` starts with `'use client'` and renders its own `&lt;html&gt;`/`&lt;body&gt;`, and `npm run build` completes with exit code 0.</done>
</task>

<task type="auto">
  <name>Task 3: Fix hardcoded copy and low-contrast link (HYG-03)</name>
  <files>apps/web/src/components/lead-form.tsx, apps/web/src/app/(admin)/admin/clients/[id]/page.tsx</files>
  <read_first>
    - `apps/web/src/components/lead-form.tsx` line 95 currently reads: `Watch for a call or text from a local 312 number.` — hardcodes a Chicago area code that is wrong for clients outside the 312 area.
    - `apps/web/src/app/(admin)/admin/clients/[id]/page.tsx` line 69 currently reads: `className="text-sm text-white/40 hover:text-gray-700"` on the "All Clients" back-link — `hover:text-gray-700` is a dark gray on a near-black background, effectively invisible on hover (contrast failure).
  </read_first>
  <action>
    In `apps/web/src/components/lead-form.tsx`, change the success-state copy from:
    `Watch for a call or text from a local 312 number.`
    to:
    `Watch for a call or text from a local number.`
    (No area code claim — the actual sending number varies per client's GHL/CallRail setup and is not knowable at form-submit time.)

    In `apps/web/src/app/(admin)/admin/clients/[id]/page.tsx`, change:
    `className="text-sm text-white/40 hover:text-gray-700"`
    to:
    `className="text-sm text-white/40 hover:text-white"`
    (Readable light color on hover, consistent with the `text-white/40` base used elsewhere on this page.)
  </action>
  <verify>
    <automated>! grep -q "312 number" apps/web/src/components/lead-form.tsx && ! grep -rq "hover:text-gray-700" "apps/web/src/app/(admin)/admin/clients/[id]/page.tsx" && grep -q "hover:text-white" "apps/web/src/app/(admin)/admin/clients/[id]/page.tsx" && echo PASS</automated>
  </verify>
  <done>No occurrence of "312 number" remains in `lead-form.tsx`; no occurrence of `hover:text-gray-700` remains in the client detail page; the back-link now uses `hover:text-white`.</done>
</task>

<task type="auto">
  <name>Task 4: Correct README and ROADMAP doc accuracy (HYG-04)</name>
  <files>apps/web/README.md, .planning/ROADMAP.md</files>
  <read_first>
    - `apps/web/README.md` has an uncommitted rewrite already staged in the working tree (`git diff --stat apps/web/README.md` shows +321/-20) — this task edits that already-rewritten file, it does not start from the old `create-next-app` boilerplate.
    - README line 5 currently reads: "HVAC companies pay a monthly retainer (or per-lead) and get managed..." — this contradicts `.planning/REQUIREMENTS.md` and CLAUDE.md, both of which state the business model is a monthly retainer only; per-lead billing is explicitly listed as Out of Scope in REQUIREMENTS.md.
    - README's "Database" section migrations list only shows `001_initial_schema.sql`, `002_add_slug_to_clients.sql`, `003_add_notifications_enabled.sql` — but `supabase/migrations/` actually contains 9 files through `009_schema_integrity.sql` (verified via `ls supabase/migrations/`).
    - README's system-architecture diagram and FastAPI description (`api/webhooks/ghl`, `api/webhooks/stripe`) omit the CallRail webhook — but `api/routers/callrail_webhooks.py` defines `POST /api/webhooks/callrail` (MISS-01..04, built in Phase 5) and it is live.
    - README's Environment Variables table documents `GHL_PRIVATE_TOKEN` as if it were the primary token, but `apps/web/.env.example` and `apps/web/src/app/api/leads/submit/route.ts` (line 100) show it is now a fallback-only value — the primary token is `clients.ghl_private_token_encrypted`, per-client (FIX-01, Phase 6). The table is also missing `CALLRAIL_WEBHOOK_SECRET` (defined in `api/.env.example`, read by `api/services/callrail_service.py` line 36) and `GHL_TOKEN_ENC_KEY` (defined in `apps/web/.env.example`).
    - `.planning/ROADMAP.md` Progress table (near the end of the file) has stale rows: Phase 1 shows no completion date; Phase 2 shows "4/5 | In Progress" but all 5 of its plans in the Phase 2 plan list are checked `[x]` (5/5, should read Complete); Phase 5 shows "1/6 | In Progress" but 5 of its 6 plans are checked (should read "5/6 | In Progress"); Phase 6 shows "5/6 | Complete" but all 6 of its plans are checked (should read "6/6"); Phase 7 shows "0/4 | Complete" but all 4 of its plans are checked (should read "4/4"). The Phase list above the table (`- [ ] **Phase 1: Foundation**` and `- [ ] **Phase 2: Lead Pipeline**`) also has Phase 1 unchecked despite its 3/3 plans being complete.
  </read_first>
  <action>
    In `apps/web/README.md`:
    1. Line 5: remove the parenthetical `(or per-lead)` so the sentence reads "HVAC companies pay a monthly retainer and get managed Google Local Services Ads, ...".
    2. In the "Database" section, replace the 3-line migrations list with all 9 entries, one line each, matching the actual files in `supabase/migrations/`: `001_initial_schema.sql`, `002_add_slug_to_clients.sql`, `003_add_notifications_enabled.sql`, `004_enable_rls_client_users.sql`, `005_add_leads_callrail_call_id_unique.sql`, `006_add_ghl_token_to_clients.sql`, `007_add_reviews_to_clients.sql`, `008_least_privilege_rls.sql`, `009_schema_integrity.sql` — with a short (3-8 word) description of each, consistent with the existing style.
    3. In the system-architecture diagram/description and the FastAPI row of the "System Architecture" table, add the CallRail webhook alongside the existing GHL and Stripe webhooks (e.g. add a line `• /api/webhooks/callrail (missed-call text-back)` next to the existing `/api/webhooks/ghl` and `/api/webhooks/stripe` lines).
    4. In the "Environment Variables" table: change the `GHL_PRIVATE_TOKEN` row's Notes to clarify it is the fallback-only token (per-client tokens are stored encrypted on `clients.ghl_private_token_encrypted`); add a new row for `CALLRAIL_WEBHOOK_SECRET` (used by: api, notes: "Signing secret for CallRail webhook signature verification"); add a new row for `GHL_TOKEN_ENC_KEY` (used by: web, notes: "AES-256-GCM key for encrypting per-client GHL tokens").

    In `.planning/ROADMAP.md`:
    5. Change `- [ ] **Phase 1: Foundation**` to `- [x] **Phase 1: Foundation**` (3/3 plans complete).
    6. Change `- [ ] **Phase 2: Lead Pipeline**` to `- [x] **Phase 2: Lead Pipeline**` (5/5 plans complete — all 5 plans under "### Phase 2" are checked `[x]`).
    7. In the Progress table: change the Phase 2 row from `| 2. Lead Pipeline | 4/5 | In Progress|  |` to `| 2. Lead Pipeline | 5/5 | Complete |  |`.
    8. In the Progress table: change the Phase 5 row from `| 5. Critical Security & Launch Blockers | 1/6 | In Progress|  |` to `| 5. Critical Security & Launch Blockers | 5/6 | In Progress|  |`.
    9. In the Progress table: change the Phase 6 row from `| 6. High-Priority Correctness, Legal & SEO | 5/6 | Complete    | 2026-07-06 |` to `| 6. High-Priority Correctness, Legal & SEO | 6/6 | Complete    | 2026-07-06 |`.
    10. In the Progress table: change the Phase 7 row from `| 7. Medium-Priority Hardening | 0/4 | Complete    | 2026-07-06 |` to `| 7. Medium-Priority Hardening | 4/4 | Complete    | 2026-07-06 |`.

    Do not change the checkbox or Progress-table state for Phases 3, 4, 8, or 9 — they are already accurate.
  </action>
  <verify>
    <automated>! grep -q "or per-lead" apps/web/README.md && grep -q "009_schema_integrity.sql" apps/web/README.md && grep -q "webhooks/callrail" apps/web/README.md && grep -q "CALLRAIL_WEBHOOK_SECRET" apps/web/README.md && grep -q "\[x\] \*\*Phase 1: Foundation\*\*" .planning/ROADMAP.md && grep -q "\[x\] \*\*Phase 2: Lead Pipeline\*\*" .planning/ROADMAP.md && grep -q "5/5 | Complete" .planning/ROADMAP.md && grep -q "6/6 | Complete" .planning/ROADMAP.md && grep -q "4/4 | Complete" .planning/ROADMAP.md && echo PASS</automated>
  </verify>
  <done>README has no per-lead billing claim, lists all 9 migrations, mentions the CallRail webhook, and documents CALLRAIL_WEBHOOK_SECRET + GHL_TOKEN_ENC_KEY; ROADMAP.md Phase 1/2 checkboxes and the Progress table rows for Phases 1, 2, 5, 6, 7 are internally consistent with their plan checklists.</done>
</task>

</tasks>

<verification>
Run from the repo root, all must pass:
1. `test ! -e "CLAUDE (1).md" && test ! -d apps/web/.git && test ! -f apps/web/public/next.svg && grep -qx "TRADEFLOW-DESIGN-A-ALL-FILES/" .gitignore && echo PASS`
2. `cd apps/web && npm run build` — exits 0, `not-found.tsx` and `global-error.tsx` compile as part of the App Router build.
3. `! grep -q "312 number" apps/web/src/components/lead-form.tsx && ! grep -q "hover:text-gray-700" "apps/web/src/app/(admin)/admin/clients/[id]/page.tsx" && echo PASS`
4. `! grep -q "or per-lead" apps/web/README.md && grep -q "webhooks/callrail" apps/web/README.md && grep -q "\[x\] \*\*Phase 1: Foundation\*\*" .planning/ROADMAP.md && echo PASS`
</verification>

<success_criteria>
- No stale/cruft paths remain (`CLAUDE (1).md`, `apps/web/.git`, unused create-next-app SVGs); `TRADEFLOW-DESIGN-A-ALL-FILES/` is gitignored.
- `apps/web/src/app/not-found.tsx` and `apps/web/src/app/global-error.tsx` exist, are on-brand, and `npm run build` passes.
- No "312 number" literal remains anywhere in the lead form; the admin back-link uses a readable hover color.
- `apps/web/README.md` and `.planning/ROADMAP.md` are internally consistent and describe only shipped features.
</success_criteria>

<output>
After completion, create `.planning/phases/08-low-priority-hygiene/08-01-SUMMARY.md`
</output>
