---
phase: 09-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/010_webhook_events.sql
  - api/services/webhook_events.py
  - api/routers/stripe_webhooks.py
  - api/routers/webhooks.py
  - api/routers/callrail_webhooks.py
  - scripts/replay_webhook_event.py
autonomous: true
requirements: [DUR-01]
must_haves:
  truths:
    - "Every one of the 3 FastAPI webhook handlers durably records the raw event before returning 200"
    - "A redelivered event with the same provider event id is treated as a duplicate and not reprocessed"
    - "A stored webhook_events row can be replayed on demand"
    - "If the durable insert itself fails (non-duplicate), the handler returns 5xx so the provider retries"
  artifacts:
    - path: "supabase/migrations/010_webhook_events.sql"
      provides: "webhook_events table with UNIQUE(provider, provider_event_id) dedup + RLS enabled, no policies"
      contains: "create table if not exists public.webhook_events"
    - path: "api/services/webhook_events.py"
      provides: "record_event / mark_processed / mark_failed / run_processing / reprocess_event"
      contains: "def record_event"
    - path: "scripts/replay_webhook_event.py"
      provides: "CLI replay entrypoint that reprocesses a stored event by id"
  key_links:
    - from: "api/routers/stripe_webhooks.py"
      to: "api/services/webhook_events.py"
      via: "record_event(...) before add_task, run_processing in background"
      pattern: "record_event"
    - from: "api/routers/callrail_webhooks.py"
      to: "webhook_events table"
      via: "record_event before returning 200"
      pattern: "record_event"
---

<objective>
Make every FastAPI webhook handler durably record the raw provider event (deduped on the provider's event id) BEFORE returning 200, process it in a BackgroundTask that flips the stored row to processed/failed, and support replaying any stored event on demand (DUR-01).

Purpose: No inbound webhook is ever silently lost; every event is auditable and replayable.
Output: migration 010, a shared `webhook_events` service, all 3 handlers converted to record-before-ACK, and a replay script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-features/09-CONTEXT.md

<interfaces>
Existing background dispatch entrypoints this plan wraps (do not change their signatures):
- api/services/stripe_service.py → `async def process_stripe_event(event: dict) -> None` (reads event["type"], event["data"]["object"])
- api/routers/webhooks.py → `async def process_ghl_event(event_type: str, payload: dict) -> None`
- api/routers/callrail_webhooks.py → `async def process_callrail_event(event: CallRailEvent) -> None`
- api/services/callrail_service.py → `class CallRailEvent(BaseModel)` (build from a dict payload)
- api/services/supabase_client.py → `get_supabase()` (sync service-role client; wrap calls in `run_in_threadpool`)
Migrations: latest on disk is 009. This plan owns 010 only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the webhook_events migration (010)</name>
  <files>supabase/migrations/010_webhook_events.sql</files>
  <read_first>supabase/migrations/009_schema_integrity.sql (SQL header/style), supabase/migrations/001_initial_schema.sql (gen_random_uuid + RLS enable style), supabase/migrations/004_enable_rls_client_users.sql (RLS-enabled-no-write pattern)</read_first>
  <action>
Create `supabase/migrations/010_webhook_events.sql` with a leading comment `-- 010_webhook_events.sql -- DUR-01: durable webhook event log + dedup + replay`. Statements:
- `create table if not exists public.webhook_events (` with columns: `id uuid primary key default gen_random_uuid()`, `provider text not null check (provider in ('stripe','ghl','callrail'))`, `provider_event_id text not null`, `event_type text`, `payload jsonb not null`, `signature_verified boolean not null default false`, `status text not null default 'received' check (status in ('received','processed','failed'))`, `error text`, `created_at timestamptz not null default now()`, `processed_at timestamptz`, and a table-level `unique (provider, provider_event_id)`.
- `alter table public.webhook_events enable row level security;` with a comment noting NO policies are added — the service role bypasses RLS, and anon/authenticated get deny-all (service-role only, per D-decision in 09-CONTEXT).
- `create index if not exists webhook_events_provider_status_idx on public.webhook_events(provider, status);`
Match the lowercase-SQL, `if not exists` style of migration 009.
  </action>
  <acceptance_criteria>
- `grep -q "create table if not exists public.webhook_events" supabase/migrations/010_webhook_events.sql`
- `grep -q "unique (provider, provider_event_id)" supabase/migrations/010_webhook_events.sql`
- `grep -q "enable row level security" supabase/migrations/010_webhook_events.sql`
- `grep -q "status in ('received','processed','failed')" supabase/migrations/010_webhook_events.sql`
- No file numbered 010 already existed before this task (this is the only new 010).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "unique (provider, provider_event_id)" supabase/migrations/010_webhook_events.sql && echo OK</automated>
  </verify>
  <done>webhook_events migration exists with the unique dedup constraint, RLS enabled, no client policies, and the status check.</done>
</task>

<task type="auto">
  <name>Task 2: Build the webhook_events service (record / mark / dispatch / replay)</name>
  <files>api/services/webhook_events.py</files>
  <read_first>api/services/supabase_client.py, api/services/stripe_service.py (process_stripe_event), api/routers/webhooks.py (process_ghl_event), api/routers/callrail_webhooks.py (process_callrail_event), api/services/callrail_service.py (CallRailEvent)</read_first>
  <action>
Create `api/services/webhook_events.py` (module-level `logger = logging.getLogger(__name__)`, `from fastapi.concurrency import run_in_threadpool`, `from datetime import datetime, timezone`, `from typing import Optional`, `from api.services.supabase_client import get_supabase`). Implement:
- `async def record_event(provider: str, provider_event_id: str, event_type: str, payload: dict, signature_verified: bool) -> Optional[str]:` — inserts a row `{provider, provider_event_id, event_type, payload, signature_verified, status: 'received'}` via `run_in_threadpool(lambda: get_supabase().table("webhook_events").insert({...}).execute())`. On success return `result.data[0]["id"]`. In `except Exception as e:` — if `"duplicate" in str(e).lower() or "unique" in str(e).lower()` return `None` (duplicate → caller skips), otherwise re-`raise` (real DB failure → caller returns 5xx).
- `async def mark_processed(event_row_id: str) -> None:` — update row `{status: 'processed', processed_at: datetime.now(timezone.utc).isoformat()}` where id == event_row_id (via run_in_threadpool). Swallow+log exceptions.
- `async def mark_failed(event_row_id: str, error: str) -> None:` — update row `{status: 'failed', error: error[:2000], processed_at: ...}`. Swallow+log exceptions.
- `async def run_processing(provider: str, event_row_id: str, payload: dict) -> None:` — in a try: dispatch by provider using LAZY imports (inside the function, to avoid circular imports): stripe → `from api.services.stripe_service import process_stripe_event; await process_stripe_event(payload)`; ghl → `from api.routers.webhooks import process_ghl_event; await process_ghl_event(payload.get("type") or payload.get("event_type", ""), payload)`; callrail → `from api.services.callrail_service import CallRailEvent; from api.routers.callrail_webhooks import process_callrail_event; await process_callrail_event(CallRailEvent(**payload))`. On success `await mark_processed(event_row_id)`. In `except Exception as e:` log with `logger.exception` and `await mark_failed(event_row_id, str(e))` (never re-raise from a background task).
- `async def reprocess_event(event_row_id: str) -> None:` — load the row (`select("*").eq("id", event_row_id)`), raise `ValueError` if not found, then `await run_processing(row["provider"], event_row_id, row["payload"])`.
  </action>
  <acceptance_criteria>
- `python -m py_compile api/services/webhook_events.py` exits 0.
- `grep -Eq "async def (record_event|mark_processed|mark_failed|run_processing|reprocess_event)" api/services/webhook_events.py` matches all five (verify each name present).
- `grep -q "duplicate" api/services/webhook_events.py` (duplicate handling present).
- `grep -q "run_in_threadpool" api/services/webhook_events.py` (DB calls offloaded, HARD-01 compliance).
- Lazy imports used: `grep -q "from api.routers.webhooks import process_ghl_event" api/services/webhook_events.py`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow && python -m py_compile api/services/webhook_events.py && echo OK</automated>
  </verify>
  <done>The service records, dedups, marks status, dispatches by provider, and replays a stored event; py_compile passes.</done>
</task>

<task type="auto">
  <name>Task 3: Convert all 3 handlers to record-before-ACK + add replay script</name>
  <files>api/routers/stripe_webhooks.py, api/routers/webhooks.py, api/routers/callrail_webhooks.py, scripts/replay_webhook_event.py</files>
  <read_first>api/routers/stripe_webhooks.py, api/routers/webhooks.py, api/routers/callrail_webhooks.py, api/services/webhook_events.py (from Task 2), api/main.py (init_supabase pattern for the script)</read_first>
  <action>
In each handler, AFTER signature verification and payload parse but BEFORE `background_tasks.add_task(...)`/return, insert-before-ACK. Add `import hashlib` where a body-hash fallback is needed, and `from api.services.webhook_events import record_event, run_processing`.

stripe_webhooks.py (`stripe_webhook`): after `event = stripe.Webhook.construct_event(...)` succeeds, do:
```
try:
    event_row_id = await record_event("stripe", event["id"], event["type"], dict(event), True)
except Exception:
    logger.exception("[stripe-webhook] durable record failed")
    raise HTTPException(status_code=503, detail="Could not record event")
if event_row_id is None:
    return {"status": "duplicate"}
background_tasks.add_task(run_processing, "stripe", event_row_id, dict(event))
return {"status": "received"}
```
Remove the old `background_tasks.add_task(process_stripe_event, event)` line and its now-unused import if unused.

webhooks.py (`ghl_webhook`): after `payload = json.loads(body)` and `event_type = ...`, compute a stable dedup id: `provider_event_id = str(payload.get("webhookId") or payload.get("id") or payload.get("messageId") or hashlib.sha256(body).hexdigest())`. Then the same record_event/duplicate/5xx pattern with provider `"ghl"`, and `background_tasks.add_task(run_processing, "ghl", event_row_id, payload)`. Remove the old `add_task(process_ghl_event, ...)`. Keep `process_ghl_event` defined (run_processing calls it).

callrail_webhooks.py (`callrail_webhook`): after `event = CallRailEvent(**payload)` parses, record with provider `"callrail"`, `provider_event_id=event.id`, `event_type="call"`, `payload=payload` (the raw dict, not the model). Same duplicate/5xx pattern, then `background_tasks.add_task(run_processing, "callrail", event_row_id, payload)`. Remove the old `add_task(process_callrail_event, event)`. Keep `process_callrail_event` defined.

Create `scripts/replay_webhook_event.py` (PEP-723-free plain script) that: loads dotenv if present, calls `api.services.supabase_client.init_supabase()`, reads the event id from `sys.argv[1]`, and `asyncio.run(reprocess_event(event_id))`, printing the resulting status. Add a top docstring documenting usage: `python -m scripts.replay_webhook_event <webhook_events.id>` (run from repo root).
  </action>
  <acceptance_criteria>
- `python -m py_compile api/routers/stripe_webhooks.py api/routers/webhooks.py api/routers/callrail_webhooks.py scripts/replay_webhook_event.py` exits 0.
- `grep -c "record_event" api/routers/stripe_webhooks.py api/routers/webhooks.py api/routers/callrail_webhooks.py` shows a match in all three files.
- `grep -q "return {\"status\": \"duplicate\"}" api/routers/stripe_webhooks.py` (dedup short-circuit present) — same check in the other two handlers.
- `grep -q "status_code=503" api/routers/callrail_webhooks.py` (5xx on durable-insert failure) — same in the other two.
- `grep -q "run_processing" api/routers/webhooks.py` (background dispatch rewired).
- `grep -q "reprocess_event" scripts/replay_webhook_event.py`.
- Old direct dispatch removed: `grep -c "add_task(process_stripe_event" api/routers/stripe_webhooks.py` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow && python -m py_compile api/routers/stripe_webhooks.py api/routers/webhooks.py api/routers/callrail_webhooks.py scripts/replay_webhook_event.py && echo OK</automated>
  </verify>
  <done>All 3 handlers record-before-ACK with dedup + 5xx-on-failure; background work runs via run_processing and flips status; a replay script reprocesses a stored event by id.</done>
</task>

</tasks>

<verification>
- `python -m py_compile api/services/webhook_events.py api/routers/stripe_webhooks.py api/routers/webhooks.py api/routers/callrail_webhooks.py scripts/replay_webhook_event.py` exits 0.
- migration 010 contains the unique dedup constraint and RLS-enabled-no-policy table.
- All 3 handlers call record_event before add_task and return 200 only after a successful (non-duplicate) record.
</verification>

<success_criteria>
DUR-01 satisfied: raw events are durably recorded (deduped on provider event id) before 200, processed in the background with status transitions, replayable via the script, and durable-insert failures surface as 5xx for provider retry.
</success_criteria>

<output>
After completion, create `.planning/phases/09-features/09-01-SUMMARY.md`.
</output>
