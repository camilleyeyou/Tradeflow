---
phase: 09-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/requirements.txt
  - api/main.py
  - api/.env.example
  - apps/web/package.json
  - apps/web/instrumentation.ts
  - apps/web/instrumentation-client.ts
  - apps/web/next.config.ts
  - apps/web/.env.example
  - .planning/phases/05-critical-security-launch-blockers/DEPLOY-CHECKLIST.md
autonomous: true
requirements: [OBSV-01, OBSV-02]
must_haves:
  truths:
    - "Unhandled errors on the FastAPI service are captured by Sentry when SENTRY_DSN is set, and Sentry is a no-op when it is unset"
    - "Unhandled errors on the Next.js app (server, edge, client) are captured by Sentry when the DSN env is set, and disabled when unset"
    - "The deploy checklist documents Supabase automated backups/PITR and an uptime check pinging /health, plus the new env vars"
  artifacts:
    - path: "api/requirements.txt"
      provides: "pinned sentry-sdk re-added (removed in Phase 7)"
      contains: "sentry-sdk"
    - path: "api/main.py"
      provides: "guarded sentry_sdk.init"
      contains: "sentry_sdk.init"
    - path: "apps/web/instrumentation.ts"
      provides: "server/edge Sentry init + onRequestError"
    - path: "apps/web/instrumentation-client.ts"
      provides: "browser Sentry init guarded by NEXT_PUBLIC_SENTRY_DSN"
  key_links:
    - from: "api/main.py"
      to: "SENTRY_DSN env"
      via: "os.environ.get guard before sentry_sdk.init"
      pattern: "SENTRY_DSN"
    - from: "apps/web/instrumentation.ts"
      to: "@sentry/nextjs"
      via: "import + Sentry.init guarded by SENTRY_DSN"
      pattern: "@sentry/nextjs"
---

<objective>
Add error observability to both apps (OBSV-01) and document backups + uptime (OBSV-02). Sentry initializes only when its DSN env var is present, so unset = fully disabled and builds/boots never break.

Purpose: Production errors surface instead of vanishing; data loss and downtime are guarded by documented backups and an uptime probe.
Output: sentry-sdk on FastAPI, @sentry/nextjs on Next.js, guarded init in both, and an expanded DEPLOY-CHECKLIST.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-features/09-CONTEXT.md

<interfaces>
- api/main.py currently: imports, `lifespan` (validate_required_env + init_supabase), then `app = FastAPI(...)` + router includes. Add Sentry init at module top, before `app = FastAPI(...)`.
- apps/web/instrumentation.ts currently only defines `register()` that calls `validateEnv()` on nodejs runtime. Extend it — do NOT drop the existing env validation.
- Next.js 15 + Turbopack (`next build --turbopack`). Browser Sentry config must live in `instrumentation-client.ts` (Next 15 convention), not `sentry.client.config.ts`.
- CLAUDE.md note: shadcn/React 19 peer deps → install npm packages with `--legacy-peer-deps`.
- Phase 7 removed `sentry-sdk` from api/requirements.txt (per STATE.md 07-02) — this re-adds it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FastAPI Sentry — re-add sentry-sdk and init guarded in main.py</name>
  <files>api/requirements.txt, api/main.py, api/.env.example</files>
  <read_first>api/requirements.txt, api/main.py, api/config.py</read_first>
  <action>
Add a pinned Sentry SDK line to `api/requirements.txt`: `sentry-sdk[fastapi]==2.39.0` (if that exact version fails to resolve during `pip install`, use the newest available 2.x and record the exact version in the SUMMARY).
In `api/main.py`, at module top (after the existing imports, before `app = FastAPI(...)`), add:
```
import os
import sentry_sdk

_sentry_dsn = os.environ.get("SENTRY_DSN")
if _sentry_dsn:
    sentry_sdk.init(
        dsn=_sentry_dsn,
        traces_sample_rate=float(os.environ.get("SENTRY_TRACES_SAMPLE_RATE", "0.1")),
        environment=os.environ.get("RAILWAY_ENVIRONMENT", "production"),
    )
```
The `if _sentry_dsn:` guard means unset DSN = no-op (no init, no error). Do NOT add SENTRY_DSN to `api/config.py` REQUIRED_ENV_VARS — it is optional.
Append to `api/.env.example` under a new `# --- Phase 9: Observability (OBSV-01) ---` header: `SENTRY_DSN=` and `SENTRY_TRACES_SAMPLE_RATE=0.1`.
  </action>
  <acceptance_criteria>
- `grep -q "sentry-sdk" api/requirements.txt`
- `python -m py_compile api/main.py` exits 0.
- `grep -q "sentry_sdk.init" api/main.py` and `grep -q "SENTRY_DSN" api/main.py`
- `grep -q "if _sentry_dsn" api/main.py` (guarded — unset DSN is a no-op).
- `grep -q "SENTRY_DSN" api/.env.example`
- SENTRY_DSN is NOT added to REQUIRED_ENV_VARS: `grep -c "SENTRY_DSN" api/config.py` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow && python -m py_compile api/main.py && grep -q "sentry-sdk" api/requirements.txt && echo OK</automated>
  </verify>
  <done>FastAPI initializes Sentry only when SENTRY_DSN is set; sentry-sdk is pinned back in requirements; py_compile passes.</done>
</task>

<task type="auto">
  <name>Task 2: Next.js Sentry — install, extend instrumentation, add client + config, env</name>
  <files>apps/web/package.json, apps/web/instrumentation.ts, apps/web/instrumentation-client.ts, apps/web/next.config.ts, apps/web/.env.example</files>
  <read_first>apps/web/instrumentation.ts, apps/web/next.config.ts, apps/web/package.json, apps/web/src/lib/env.ts</read_first>
  <action>
Install the SDK: from `apps/web`, run `npm install --legacy-peer-deps @sentry/nextjs` (this updates package.json + lockfile).
Extend `apps/web/instrumentation.ts` — KEEP the existing `validateEnv()` call. After it, add Sentry server/edge init and export `onRequestError`:
```
import * as Sentry from '@sentry/nextjs'

export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    const { validateEnv } = await import('@/lib/env')
    validateEnv()
  }
  if (process.env.SENTRY_DSN) {
    Sentry.init({
      dsn: process.env.SENTRY_DSN,
      tracesSampleRate: Number(process.env.SENTRY_TRACES_SAMPLE_RATE ?? 0.1),
    })
  }
}

export const onRequestError = Sentry.captureRequestError
```
Create `apps/web/instrumentation-client.ts`:
```
import * as Sentry from '@sentry/nextjs'

if (process.env.NEXT_PUBLIC_SENTRY_DSN) {
  Sentry.init({
    dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
    tracesSampleRate: Number(process.env.NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE ?? 0.1),
  })
}
export const onRouterTransitionStart = Sentry.captureRouterTransitionStart
```
Wrap `apps/web/next.config.ts` with `withSentryConfig` so it is inert without an auth token/DSN:
```
import { withSentryConfig } from '@sentry/nextjs'
// ...existing nextConfig...
export default withSentryConfig(nextConfig, { silent: true })
```
Append to `apps/web/.env.example` under `# --- Phase 9: Observability (OBSV-01) ---`: `SENTRY_DSN=`, `NEXT_PUBLIC_SENTRY_DSN=`, `SENTRY_TRACES_SAMPLE_RATE=0.1`, `NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1`. Do NOT add these to `src/lib/env.ts` serverEnvSchema (they are optional).
  </action>
  <acceptance_criteria>
- `grep -q "@sentry/nextjs" apps/web/package.json`
- `grep -q "@sentry/nextjs" apps/web/instrumentation.ts` and `grep -q "onRequestError" apps/web/instrumentation.ts`
- `grep -q "validateEnv" apps/web/instrumentation.ts` (existing behavior preserved).
- `test -f apps/web/instrumentation-client.ts && grep -q "NEXT_PUBLIC_SENTRY_DSN" apps/web/instrumentation-client.ts`
- `grep -q "withSentryConfig" apps/web/next.config.ts`
- `grep -q "SENTRY_DSN" apps/web/.env.example`
- `cd apps/web && npx tsc --noEmit` exits 0.
- `cd apps/web && npm run build` exits 0 (build succeeds with DSN unset = Sentry disabled).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && npm run build && echo OK</automated>
  </verify>
  <done>Next.js captures server/edge/client errors when the DSN env is set; existing env validation preserved; tsc + build pass with Sentry disabled.</done>
</task>

<task type="auto">
  <name>Task 3: OBSV-02 — document backups + uptime + new env vars in DEPLOY-CHECKLIST</name>
  <files>.planning/phases/05-critical-security-launch-blockers/DEPLOY-CHECKLIST.md</files>
  <read_first>.planning/phases/05-critical-security-launch-blockers/DEPLOY-CHECKLIST.md</read_first>
  <action>
Append a new section `## 6. Observability, Backups & Uptime (Phase 9 — OBSV-01/02)` to the checklist. Include:
- A new env-var subtable listing: `SENTRY_DSN` (FastAPI + Next.js server; obtain from Sentry project → Client Keys DSN), `NEXT_PUBLIC_SENTRY_DSN` (Next.js browser), `SENTRY_TRACES_SAMPLE_RATE` / `NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE` (optional, default 0.1), and `ANTHROPIC_API_KEY` (Next.js lead scoring, Phase 9 AI-01 — obtain from Anthropic Console; scoring is skipped when unset). Note all are optional/feature-flag env vars.
- A "Supabase Backups (OBSV-02 — requires human)" subsection: enable automated daily backups and Point-in-Time Recovery in Supabase Dashboard → Project → Database → Backups; note PITR requires a paid plan; document the retention window chosen.
- An "Uptime Check (OBSV-02)" subsection: `GET https://<railway-host>/health` is the uptime target; configure an external monitor (e.g. UptimeRobot/BetterStack/Cronitor) to ping it every 1–5 minutes and alert on non-200 or >5s latency.
- Extend the "Migration Apply Order" table (section 2) with rows for `010_webhook_events.sql` (DUR-01), `011_add_first_contact_at.sql` (ROI-01), and `012_add_urgency.sql` (AI-01), applied after 009 in numeric order.
  </action>
  <acceptance_criteria>
- `grep -qi "backup" DEPLOY-CHECKLIST.md` (the section 6 backups subsection).
- `grep -q "/health" DEPLOY-CHECKLIST.md` (referenced as uptime target in the new section).
- `grep -q "SENTRY_DSN" DEPLOY-CHECKLIST.md`
- `grep -q "ANTHROPIC_API_KEY" DEPLOY-CHECKLIST.md`
- `grep -q "010_webhook_events.sql" DEPLOY-CHECKLIST.md`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/.planning/phases/05-critical-security-launch-blockers && grep -qi "backup" DEPLOY-CHECKLIST.md && grep -q "010_webhook_events.sql" DEPLOY-CHECKLIST.md && grep -q "ANTHROPIC_API_KEY" DEPLOY-CHECKLIST.md && echo OK</automated>
  </verify>
  <done>The checklist documents Sentry/Anthropic env vars, Supabase backups/PITR, an uptime probe of /health, and the new migration apply order.</done>
</task>

</tasks>

<verification>
- `python -m py_compile api/main.py` exits 0 and requirements contains sentry-sdk.
- `cd apps/web && npx tsc --noEmit && npm run build` both exit 0 with Sentry disabled (no DSN).
- DEPLOY-CHECKLIST documents backups, /health uptime, and the new env vars + migrations.
</verification>

<success_criteria>
OBSV-01: both apps capture unhandled errors via Sentry, guarded by DSN env so unset = disabled. OBSV-02: backups + uptime + new env vars documented in the deploy checklist.
</success_criteria>

<output>
After completion, create `.planning/phases/09-features/09-02-SUMMARY.md`.
</output>
