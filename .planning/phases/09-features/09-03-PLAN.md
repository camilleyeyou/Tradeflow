---
phase: 09-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/011_add_first_contact_at.sql
  - apps/web/src/lib/supabase/types.ts
  - apps/web/src/lib/types/dashboard.ts
  - apps/web/src/lib/actions/lead-actions.ts
  - apps/web/src/components/dashboard/roi-summary.tsx
  - apps/web/src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements: [ROI-01, ROI-02]
must_haves:
  truths:
    - "Each lead records first_contact_at the first time it moves off status 'new' (only when still null)"
    - "The dashboard overview shows this-month lead count, an estimated lead value, and average speed-to-lead"
  artifacts:
    - path: "supabase/migrations/011_add_first_contact_at.sql"
      provides: "leads.first_contact_at timestamptz null"
      contains: "first_contact_at"
    - path: "apps/web/src/components/dashboard/roi-summary.tsx"
      provides: "monthly ROI summary card (count, estimated value, avg speed-to-lead)"
    - path: "apps/web/src/lib/actions/lead-actions.ts"
      provides: "updateLeadStatus sets first_contact_at on first transition off 'new'"
      contains: "first_contact_at"
  key_links:
    - from: "apps/web/src/lib/actions/lead-actions.ts"
      to: "leads.first_contact_at"
      via: "conditional update when status leaves 'new' and first_contact_at is null"
      pattern: "first_contact_at"
    - from: "apps/web/src/app/(dashboard)/dashboard/page.tsx"
      to: "apps/web/src/components/dashboard/roi-summary.tsx"
      via: "server component renders RoiSummary with month-scoped leads"
      pattern: "RoiSummary"
---

<objective>
Record time-to-first-contact per lead (ROI-01) and surface a monthly ROI summary on the dashboard overview (ROI-02): lead count this month, estimated lead value, and average speed-to-lead.

Purpose: HVAC owners see the retainer paying off — how many leads, their estimated value, and how fast leads are being contacted.
Output: migration 011, first_contact_at wiring in updateLeadStatus, and a server-rendered ROI summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-features/09-CONTEXT.md

<interfaces>
- apps/web/src/lib/actions/lead-actions.ts → `updateLeadStatus(leadId, status)`: 'use server', auth via getUser(), then `supabase.from('leads').update({ status }).eq('id', leadId)`. RLS enforces ownership.
- apps/web/src/lib/types/dashboard.ts → `interface Lead` (add `first_contact_at: string | null`). `LEAD_STATUSES = ['new','contacted','booked','completed','lost']`.
- apps/web/src/lib/supabase/types.ts → hand-authored `Database`; leads Row/Insert/Update need `first_contact_at?: string | null`.
- Overview page apps/web/src/app/(dashboard)/dashboard/page.tsx: server component, `export const dynamic = 'force-dynamic'`, fetches leads via RLS-scoped `createClient()` from '@/lib/supabase/server', currently selects `id, homeowner_name, service_type, status, created_at`.
- Existing card style: apps/web/src/components/dashboard/count-cards.tsx uses `@/components/ui/card` (Card/CardHeader/CardTitle/CardContent) + lucide icons; brand is black+gold, mobile grid `grid-cols-2 lg:grid-cols-4`.
- Migrations: this plan owns 011 only (010 belongs to plan 09-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 011 + add first_contact_at to types</name>
  <files>supabase/migrations/011_add_first_contact_at.sql, apps/web/src/lib/supabase/types.ts, apps/web/src/lib/types/dashboard.ts</files>
  <read_first>supabase/migrations/009_schema_integrity.sql (style), apps/web/src/lib/supabase/types.ts (leads Row/Insert/Update), apps/web/src/lib/types/dashboard.ts (Lead interface)</read_first>
  <action>
Create `supabase/migrations/011_add_first_contact_at.sql` with header comment `-- 011_add_first_contact_at.sql -- ROI-01: time-to-first-contact` and the single statement:
`alter table public.leads add column if not exists first_contact_at timestamptz;`
In `apps/web/src/lib/supabase/types.ts`, add `first_contact_at: string | null` to the leads `Row`, and `first_contact_at?: string | null` to the leads `Insert` and `Update` objects (place near contacted_at/booked_at/completed_at).
In `apps/web/src/lib/types/dashboard.ts`, add `first_contact_at: string | null` to the `Lead` interface (near contacted_at).
  </action>
  <acceptance_criteria>
- `grep -q "first_contact_at timestamptz" supabase/migrations/011_add_first_contact_at.sql`
- `grep -c "first_contact_at" apps/web/src/lib/supabase/types.ts` returns 3 (Row, Insert, Update).
- `grep -q "first_contact_at: string | null" apps/web/src/lib/types/dashboard.ts`
- `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && echo OK</automated>
  </verify>
  <done>Column exists in migration 011 and is reflected in both the Database and dashboard Lead types; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Set first_contact_at on first transition off 'new'</name>
  <files>apps/web/src/lib/actions/lead-actions.ts</files>
  <read_first>apps/web/src/lib/actions/lead-actions.ts</read_first>
  <action>
In `updateLeadStatus(leadId, status)`, after the auth check and before/within the update: build the update payload so that when the NEW status is not 'new', we also set `first_contact_at` to now — but ONLY if it is currently null (earliest-touch wins per ROI-01).
Concrete approach: first read the lead's current first_contact_at (`const { data: current } = await supabase.from('leads').select('first_contact_at').eq('id', leadId).maybeSingle()`), then:
```
const update: { status: LeadStatus; first_contact_at?: string } = { status }
if (status !== 'new' && !current?.first_contact_at) {
  update.first_contact_at = new Date().toISOString()
}
const { error } = await supabase.from('leads').update(update).eq('id', leadId)
```
Keep the existing `revalidatePath('/dashboard/leads')` and `revalidatePath('/dashboard')` calls. Do not modify `updateLeadNotes`.
  </action>
  <acceptance_criteria>
- `grep -q "first_contact_at" apps/web/src/lib/actions/lead-actions.ts`
- `grep -q "status !== 'new'" apps/web/src/lib/actions/lead-actions.ts` (only stamps on transition off new).
- `grep -q "!current?.first_contact_at" apps/web/src/lib/actions/lead-actions.ts` (only when still null — earliest wins).
- `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && grep -q "first_contact_at" src/lib/actions/lead-actions.ts && echo OK</automated>
  </verify>
  <done>updateLeadStatus stamps first_contact_at exactly once, on the first move off 'new'; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: RoiSummary component + wire into overview page</name>
  <files>apps/web/src/components/dashboard/roi-summary.tsx, apps/web/src/app/(dashboard)/dashboard/page.tsx</files>
  <read_first>apps/web/src/components/dashboard/count-cards.tsx (card + brand style), apps/web/src/app/(dashboard)/dashboard/page.tsx (data fetch pattern), apps/web/src/lib/types/dashboard.ts</read_first>
  <action>
Create `apps/web/src/components/dashboard/roi-summary.tsx` (server-safe, no 'use client'). Export a top-level `export const ESTIMATED_LEAD_VALUE = 400` constant with a comment: "Estimated value per lead (USD) — conservative booked-job estimate; clearly labeled 'estimated' in the UI." Define:
```
interface RoiSummaryProps {
  monthLeadCount: number
  estimatedValue: number       // monthLeadCount * ESTIMATED_LEAD_VALUE
  avgSpeedToLeadMinutes: number | null  // null when no contacted leads this month
}
export function RoiSummary({ monthLeadCount, estimatedValue, avgSpeedToLeadMinutes }: RoiSummaryProps) { ... }
```
Render a `grid grid-cols-1 sm:grid-cols-3 gap-4` of three Cards (reuse `@/components/ui/card`, lucide icons, black+gold): "Leads This Month" (monthLeadCount), "Estimated Value" (formatted as `$${estimatedValue.toLocaleString()}` with a small "estimated" caption), and "Avg Speed to Lead" (show `${avgSpeedToLeadMinutes} min` or `—` when null). Keep it mobile-first.

In `apps/web/src/app/(dashboard)/dashboard/page.tsx`: extend the leads select to also fetch `created_at, first_contact_at, status` (adjust the select string to include `first_contact_at`). Compute, from `leads`:
- `startOfMonth = new Date(new Date().getFullYear(), new Date().getMonth(), 1).toISOString()`
- `monthLeads = leads.filter(l => l.created_at >= startOfMonth)`
- `monthLeadCount = monthLeads.length`
- `estimatedValue = monthLeadCount * ESTIMATED_LEAD_VALUE`
- avg speed-to-lead: for month leads with a non-null `first_contact_at`, compute `(Date.parse(first_contact_at) - Date.parse(created_at)) / 60000` minutes, average them, `Math.round`; `null` if none.
Render `<RoiSummary .../>` above `<CountCards />`. Keep `export const dynamic = 'force-dynamic'`.
  </action>
  <acceptance_criteria>
- `test -f apps/web/src/components/dashboard/roi-summary.tsx`
- `grep -q "ESTIMATED_LEAD_VALUE" apps/web/src/components/dashboard/roi-summary.tsx`
- `grep -q "RoiSummary" apps/web/src/app/(dashboard)/dashboard/page.tsx`
- `grep -q "first_contact_at" apps/web/src/app/(dashboard)/dashboard/page.tsx` (fetched for speed-to-lead).
- `grep -qi "estimated" apps/web/src/components/dashboard/roi-summary.tsx` (value labeled estimated).
- `cd apps/web && npx tsc --noEmit && npm run build` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && npm run build && echo OK</automated>
  </verify>
  <done>The overview renders a mobile-responsive ROI summary (this-month count, estimated value, avg speed-to-lead) computed from first_contact_at; tsc + build pass.</done>
</task>

</tasks>

<verification>
- migration 011 adds leads.first_contact_at.
- updateLeadStatus stamps first_contact_at once on first move off 'new'.
- `cd apps/web && npx tsc --noEmit && npm run build` exit 0; overview shows the three ROI metrics.
</verification>

<success_criteria>
ROI-01: leads record time-to-first-contact. ROI-02: the dashboard overview shows monthly lead count, estimated lead value, and average speed-to-lead.
</success_criteria>

<output>
After completion, create `.planning/phases/09-features/09-03-SUMMARY.md`.
</output>
