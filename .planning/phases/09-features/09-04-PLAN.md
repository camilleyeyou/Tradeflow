---
phase: 09-features
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/ghl.ts
  - apps/web/src/app/api/leads/[id]/messages/route.ts
  - apps/web/src/components/dashboard/sms-inbox.tsx
  - apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx
  - apps/web/src/components/dashboard/leads-table.tsx
autonomous: true
requirements: [INBX-01]
must_haves:
  truths:
    - "An HVAC owner can open a lead detail page and see the SMS conversation for that lead"
    - "The owner can type and send a reply that reaches the homeowner through GHL"
    - "The route enforces auth + ownership (RLS-scoped) and never exposes GHL tokens to the browser"
  artifacts:
    - path: "apps/web/src/lib/ghl.ts"
      provides: "getConversationMessages + sendSMSReply (per-client token), graceful degradation"
      contains: "getConversationMessages"
    - path: "apps/web/src/app/api/leads/[id]/messages/route.ts"
      provides: "auth'd GET (thread) + POST (reply) route handler"
    - path: "apps/web/src/components/dashboard/sms-inbox.tsx"
      provides: "client component: thread view + reply box"
    - path: "apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx"
      provides: "lead detail server page hosting the inbox"
  key_links:
    - from: "apps/web/src/components/dashboard/sms-inbox.tsx"
      to: "/api/leads/[id]/messages"
      via: "fetch GET on mount + POST on send"
      pattern: "/api/leads/"
    - from: "apps/web/src/app/api/leads/[id]/messages/route.ts"
      to: "apps/web/src/lib/ghl.ts"
      via: "server-side getConversationMessages / sendSMSReply with decrypted per-client token"
      pattern: "sendSMSReply"
---

<objective>
Give HVAC owners a two-way SMS inbox per lead (INBX-01): view the GHL conversation thread and send a reply, all server-side auth'd and RLS-scoped, with GHL tokens never leaving the server.

Purpose: Owners can respond to homeowners without leaving the dashboard.
Output: GHL conversations service functions, an auth'd route handler, a lead detail page, and an inbox UI linked from the leads table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-features/09-CONTEXT.md

<interfaces>
- apps/web/src/lib/ghl.ts: existing `ghlFetch(path, token, options)` helper (throws on !ok), `createGHLContact`, `lookupContactByPhone`, `addContactToWorkflow`. Base `https://services.leadconnectorhq.com`, `Version: 2021-07-28`. Add the two conversation functions here reusing `ghlFetch`.
- Per-client token model (FIX-01): `clients.ghl_private_token_encrypted` (AES-256-GCM) decrypted via `decryptToken` from '@/lib/crypto'; fall back to `process.env.GHL_PRIVATE_TOKEN`. `clients.ghl_sub_account_id` is the GHL locationId.
- leads carry `ghl_contact_id` (Lead type already includes it). RLS: an owner-scoped `createClient()` from '@/lib/supabase/server' returns a lead row only if the owner owns it.
- Reading the encrypted token requires service-role: use inline `createClient` from '@supabase/supabase-js' with SUPABASE_SERVICE_ROLE_KEY (same pattern as apps/web/src/app/api/leads/submit/route.ts) AFTER ownership is verified with the RLS-scoped ssr client.
- Leads table apps/web/src/components/dashboard/leads-table.tsx is a 'use client' component listing rows; add a per-row link to `/dashboard/leads/${lead.id}`.
- Authed dashboard pages set `export const dynamic = 'force-dynamic'` and guard with `getUser()` → redirect('/login').
- Next.js 15: route handler params are async — `{ params }: { params: Promise<{ id: string }> }`, `const { id } = await params`.
- GHL Conversations API endpoints are NOT yet verified in this codebase — isolate them in ghl.ts, document the exact paths used, and degrade gracefully (return [] / false) on any non-2xx or thrown error so the dashboard never crashes if the conversations scope is unavailable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GHL conversations service (fetch thread + send reply)</name>
  <files>apps/web/src/lib/ghl.ts</files>
  <read_first>apps/web/src/lib/ghl.ts (ghlFetch + existing functions), apps/web/src/lib/crypto.ts</read_first>
  <action>
Add two exported functions to `apps/web/src/lib/ghl.ts`, reusing `ghlFetch`. Document the GHL v2 (LeadConnector) endpoints in comments and note they require confirmation of the conversations scope at deploy time.

```
export interface GHLMessage {
  id: string
  direction: 'inbound' | 'outbound'
  body: string
  dateAdded: string
}

// GET /conversations/search?locationId=&contactId=  -> conversation id
// GET /conversations/{conversationId}/messages       -> messages
export async function getConversationMessages(
  contactId: string,
  locationId: string,
  token: string
): Promise<GHLMessage[]> {
  try {
    const search = await ghlFetch(
      `/conversations/search?${new URLSearchParams({ locationId, contactId }).toString()}`,
      token,
      { method: 'GET' }
    )
    const sdata = await search.json() as { conversations?: Array<{ id?: string }> }
    const conversationId = sdata.conversations?.[0]?.id
    if (!conversationId) return []
    const res = await ghlFetch(`/conversations/${conversationId}/messages`, token, { method: 'GET' })
    const data = await res.json() as { messages?: { messages?: Array<Record<string, unknown>> } | Array<Record<string, unknown>> }
    // GHL nests messages under messages.messages in some responses — normalize both shapes.
    const raw = Array.isArray((data as any).messages) ? (data as any).messages : ((data as any).messages?.messages ?? [])
    return raw.map((m: any) => ({
      id: String(m.id ?? ''),
      direction: m.direction === 'inbound' ? 'inbound' : 'outbound',
      body: String(m.body ?? m.message ?? ''),
      dateAdded: String(m.dateAdded ?? m.dateUpdated ?? ''),
    }))
  } catch (err) {
    console.error('[ghl] getConversationMessages error:', err)
    return []
  }
}

// POST /conversations/messages  { type: 'SMS', contactId, message }
export async function sendSMSReply(
  contactId: string,
  message: string,
  token: string
): Promise<boolean> {
  try {
    await ghlFetch('/conversations/messages', token, {
      method: 'POST',
      body: JSON.stringify({ type: 'SMS', contactId, message }),
    })
    return true
  } catch (err) {
    console.error('[ghl] sendSMSReply error:', err)
    return false
  }
}
```
Do not remove or alter the existing functions.
  </action>
  <acceptance_criteria>
- `grep -q "export async function getConversationMessages" apps/web/src/lib/ghl.ts`
- `grep -q "export async function sendSMSReply" apps/web/src/lib/ghl.ts`
- `grep -q "/conversations/messages" apps/web/src/lib/ghl.ts` (documented endpoint present).
- Both functions catch errors and return `[]`/`false` (graceful degradation): `grep -q "return \[\]" apps/web/src/lib/ghl.ts`.
- `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && grep -q "sendSMSReply" src/lib/ghl.ts && echo OK</automated>
  </verify>
  <done>ghl.ts exposes thread-fetch and SMS-send behind the existing ghlFetch client, using per-client tokens and degrading gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Auth'd route handler — GET thread / POST reply</name>
  <files>apps/web/src/app/api/leads/[id]/messages/route.ts</files>
  <read_first>apps/web/src/app/api/leads/submit/route.ts (inline service-role createClient + decryptToken + GHL_PRIVATE_TOKEN fallback), apps/web/src/lib/supabase/server.ts, apps/web/src/lib/ghl.ts, apps/web/src/lib/crypto.ts</read_first>
  <action>
Create `apps/web/src/app/api/leads/[id]/messages/route.ts`. Add `export const dynamic = 'force-dynamic'`.

Shared helper `resolveLeadContext(id)`:
1. `const supabase = await createClient()` (ssr, from '@/lib/supabase/server'); `const { data: { user } } = await supabase.auth.getUser()`; if no user return 401.
2. RLS-scoped ownership check: `const { data: lead } = await supabase.from('leads').select('id, client_id, ghl_contact_id').eq('id', id).maybeSingle()`. If `!lead` return 404 (RLS hides non-owned leads → 404 is correct).
3. If `!lead.ghl_contact_id` return an empty-thread signal (contact not synced yet).
4. Read the client's GHL creds with a service-role client (inline `createClient` from '@supabase/supabase-js' with SUPABASE_SERVICE_ROLE_KEY, `auth: { persistSession: false }`): `select('ghl_sub_account_id, ghl_private_token_encrypted').eq('id', lead.client_id).single()`. token = `ghl_private_token_encrypted ? decryptToken(...) : process.env.GHL_PRIVATE_TOKEN`. Return `{ lead, locationId, token }`.

`export async function GET(request, { params }: { params: Promise<{ id: string }> })`: `const { id } = await params`. Resolve context; on 401/404 return the matching status. If no ghl_contact_id or no token/locationId, return `NextResponse.json({ messages: [] })`. Else `const messages = await getConversationMessages(lead.ghl_contact_id, locationId, token)` and return `{ messages }`.

`export async function POST(request, { params })`: parse `{ message }` from body; validate it is a non-empty string (return 400 otherwise). Resolve context; if missing ghl_contact_id/token/locationId return 409 `{ error: 'Contact not synced' }`. `const ok = await sendSMSReply(lead.ghl_contact_id, message, token)`; return `{ success: ok }` (status 502 if `!ok`).

Never include the token or encrypted token in any response body.
  </action>
  <acceptance_criteria>
- `test -f "apps/web/src/app/api/leads/[id]/messages/route.ts"`
- `grep -q "export async function GET" "apps/web/src/app/api/leads/[id]/messages/route.ts"` and `grep -q "export async function POST" "apps/web/src/app/api/leads/[id]/messages/route.ts"`
- `grep -q "getUser" "apps/web/src/app/api/leads/[id]/messages/route.ts"` (auth enforced).
- `grep -q "decryptToken" "apps/web/src/app/api/leads/[id]/messages/route.ts"` (per-client token, server-side only).
- `grep -q "await params" "apps/web/src/app/api/leads/[id]/messages/route.ts"` (Next 15 async params).
- No token leaked: `grep -c "ghl_private_token_encrypted" "apps/web/src/app/api/leads/[id]/messages/route.ts"` shows it is only read for decryption, never returned (manually confirm it is not placed in any NextResponse.json body).
- `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && test -f "src/app/api/leads/[id]/messages/route.ts" && echo OK</automated>
  </verify>
  <done>The route returns a lead's SMS thread and sends replies, enforcing auth + RLS ownership and keeping GHL tokens server-side.</done>
</task>

<task type="auto">
  <name>Task 3: Inbox UI + lead detail page + leads-table link</name>
  <files>apps/web/src/components/dashboard/sms-inbox.tsx, apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx, apps/web/src/components/dashboard/leads-table.tsx</files>
  <read_first>apps/web/src/components/dashboard/leads-table.tsx, apps/web/src/app/(dashboard)/dashboard/leads/page.tsx (page + auth pattern), apps/web/src/components/dashboard/notes-editor.tsx ('use client' + fetch pattern), apps/web/src/components/ui/{button,textarea}.tsx</read_first>
  <action>
Create `apps/web/src/components/dashboard/sms-inbox.tsx` ('use client'). Props `{ leadId: string }`. On mount (`useEffect`) `fetch('/api/leads/'+leadId+'/messages')`, store `messages` in state, render a scrollable thread (inbound left/muted, outbound right/gold), an empty state ("No messages yet."), and a loading state. Below the thread render a `Textarea` + a `Button` "Send"; on send POST `{ message }` to the same URL, and on `{ success: true }` clear the box and re-fetch the thread; on failure show an inline error. Use black+gold, mobile-first (`max-h` scroll area, full-width reply box on mobile). Wrap fetches in try/catch.

Create `apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx` (server component, `export const dynamic = 'force-dynamic'`). Signature `{ params }: { params: Promise<{ id: string }> }`; `const { id } = await params`. `const supabase = await createClient()`; `getUser()` → redirect('/login') if none. Fetch the lead (`select('*').eq('id', id).maybeSingle()`); if none, call `notFound()` from 'next/navigation'. Render a header with the homeowner name + a back link to `/dashboard/leads`, a small details block (phone, service_type, status), and `<SmsInbox leadId={id} />`.

Edit `apps/web/src/components/dashboard/leads-table.tsx`: in the Name cell (or a new trailing cell), wrap/append a Next.js `Link` from 'next/link' to `/dashboard/leads/${lead.id}` labeled "View" (or make the name a link). Keep all existing columns/behavior intact.
  </action>
  <acceptance_criteria>
- `test -f apps/web/src/components/dashboard/sms-inbox.tsx && head -1 apps/web/src/components/dashboard/sms-inbox.tsx | grep -q "use client"`
- `grep -q "/api/leads/" apps/web/src/components/dashboard/sms-inbox.tsx` (GET + POST to the handler).
- `test -f "apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx"` and `grep -q "SmsInbox" "apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx"`
- `grep -q "notFound" "apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx"`
- `grep -q "/dashboard/leads/" apps/web/src/components/dashboard/leads-table.tsx` (row link added).
- `cd apps/web && npx tsc --noEmit && npm run build` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && npm run build && echo OK</automated>
  </verify>
  <done>Owners reach a lead detail page from the leads table, see the SMS thread, and send replies; tsc + build pass.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit && npm run build` exit 0.
- The route handler enforces getUser + RLS ownership and reads per-client tokens server-side only.
- GHL calls are isolated in ghl.ts and degrade gracefully when the conversations scope is unavailable.
</verification>

<success_criteria>
INBX-01: the dashboard shows a lead's SMS conversation and lets the owner send a reply through GHL, with auth + ownership enforced and tokens never exposed to the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/09-features/09-04-SUMMARY.md`.
</output>
