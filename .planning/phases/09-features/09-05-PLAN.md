---
phase: 09-features
plan: 05
type: execute
wave: 2
depends_on: ["09-02", "09-03", "09-04"]
files_modified:
  - supabase/migrations/012_add_urgency.sql
  - apps/web/package.json
  - apps/web/.env.example
  - apps/web/src/lib/supabase/types.ts
  - apps/web/src/lib/types/dashboard.ts
  - apps/web/src/lib/scoring.ts
  - apps/web/src/app/api/leads/submit/route.ts
  - apps/web/src/app/(dashboard)/dashboard/leads/page.tsx
  - apps/web/src/components/dashboard/leads-table.tsx
autonomous: true
requirements: [AI-01, AI-02]
must_haves:
  truths:
    - "New form leads get a 1-10 urgency_score + urgency_reason written via a claude-fable-5 call, in background, never blocking the homeowner 200"
    - "Scoring is skipped gracefully (no error, no thrown exception) when ANTHROPIC_API_KEY is unset or the call fails"
    - "The dashboard leads list orders high-urgency leads first and shows a Hot badge for high scores"
  artifacts:
    - path: "supabase/migrations/012_add_urgency.sql"
      provides: "leads.urgency_score int null + leads.urgency_reason text null"
      contains: "urgency_score"
    - path: "apps/web/src/lib/scoring.ts"
      provides: "scoreLeadUrgency() using @anthropic-ai/sdk model claude-fable-5, resilient"
      contains: "claude-fable-5"
    - path: "apps/web/src/app/api/leads/submit/route.ts"
      provides: "scoring invoked inside existing after() background block"
  key_links:
    - from: "apps/web/src/app/api/leads/submit/route.ts"
      to: "apps/web/src/lib/scoring.ts"
      via: "after(): scoreLeadUrgency(...) then leads.update({urgency_score, urgency_reason})"
      pattern: "scoreLeadUrgency"
    - from: "apps/web/src/components/dashboard/leads-table.tsx"
      to: "lead.urgency_score"
      via: "Hot badge when score >= 8"
      pattern: "urgency_score"
---

<objective>
Score inbound form leads 1-10 for urgency via the Claude API (AI-01) and surface hot leads at the top of the dashboard list (AI-02). Scoring runs in the existing background `after()` block, is resilient (timeout + try/catch), and never blocks the homeowner's 200 response. Missing ANTHROPIC_API_KEY = scoring silently skipped.

Purpose: Owners see the hottest leads first without any change to the fast lead-capture path.
Output: migration 012, an Anthropic scoring module, background scoring on form leads, and Hot-lead sort + badge in the dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-features/09-CONTEXT.md

<interfaces>
- AI-01 is an Anthropic/Claude task — CONSULT THE `claude-api` SKILL for correct SDK usage before writing scoring.ts. Use `@anthropic-ai/sdk`, model id `claude-fable-5`, `client.messages.create({ model, max_tokens (small), temperature (low, ~0), system, messages })`. Parse the JSON `{score, reason}` defensively from the text content block.
- Lead-submit apps/web/src/app/api/leads/submit/route.ts already returns 200 to the homeowner BEFORE the `after(async () => { ... })` block runs GHL + Resend. The inserted lead id is `lead.id`; the service-role `supabase` client is in scope inside after(). Add scoring INSIDE the existing after() block.
- Migration 012 is owned by this plan (010 = 09-01, 011 = 09-03).
- types.ts leads Row/Insert/Update and dashboard.ts Lead already gained first_contact_at in 09-03 — ADD urgency fields alongside; do not remove 09-03's additions.
- Leads list apps/web/src/app/(dashboard)/dashboard/leads/page.tsx: server component, RLS-scoped `.from('leads').select('*').order('created_at', { ascending: false })`. leads-table.tsx (09-04 added a per-row "View" link) renders rows and uses `@/components/ui/badge`.
- CLAUDE.md: install npm packages with `--legacy-peer-deps`. Keep secrets server-only (scoring.ts is imported only from the route handler / server).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 012 + urgency fields in types + install SDK + env</name>
  <files>supabase/migrations/012_add_urgency.sql, apps/web/package.json, apps/web/.env.example, apps/web/src/lib/supabase/types.ts, apps/web/src/lib/types/dashboard.ts</files>
  <read_first>supabase/migrations/011_add_first_contact_at.sql (style, from 09-03), apps/web/src/lib/supabase/types.ts (leads Row/Insert/Update — already has first_contact_at), apps/web/src/lib/types/dashboard.ts (Lead), apps/web/.env.example</read_first>
  <action>
Create `supabase/migrations/012_add_urgency.sql` with header `-- 012_add_urgency.sql -- AI-01: Claude urgency score + reason` and:
```
alter table public.leads add column if not exists urgency_score int;
alter table public.leads add column if not exists urgency_reason text;
alter table public.leads add constraint leads_urgency_score_check
  check (urgency_score is null or (urgency_score between 1 and 10));
```
Install the SDK: from `apps/web`, `npm install --legacy-peer-deps @anthropic-ai/sdk`.
In `apps/web/src/lib/supabase/types.ts`, add `urgency_score: number | null` and `urgency_reason: string | null` to leads `Row`, and `urgency_score?: number | null` / `urgency_reason?: string | null` to leads `Insert` and `Update`.
In `apps/web/src/lib/types/dashboard.ts`, add `urgency_score: number | null` and `urgency_reason: string | null` to the `Lead` interface.
Append to `apps/web/.env.example` under `# --- Phase 9: AI lead scoring (AI-01) ---`: `ANTHROPIC_API_KEY=              # Anthropic API key; when unset, lead urgency scoring is skipped gracefully`. Do NOT add it to src/lib/env.ts required schema (optional).
  </action>
  <acceptance_criteria>
- `grep -q "urgency_score int" supabase/migrations/012_add_urgency.sql` and `grep -q "urgency_reason text" supabase/migrations/012_add_urgency.sql`
- `grep -q "between 1 and 10" supabase/migrations/012_add_urgency.sql`
- `grep -q "@anthropic-ai/sdk" apps/web/package.json`
- `grep -c "urgency_score" apps/web/src/lib/supabase/types.ts` returns 3 (Row/Insert/Update).
- `grep -q "urgency_score: number | null" apps/web/src/lib/types/dashboard.ts`
- `grep -q "ANTHROPIC_API_KEY" apps/web/.env.example`
- `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && grep -q "@anthropic-ai/sdk" package.json && echo OK</automated>
  </verify>
  <done>Migration 012 adds the two columns with a 1-10 check; the SDK is installed; types + env updated; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Anthropic scoring module + wire into lead-submit after()</name>
  <files>apps/web/src/lib/scoring.ts, apps/web/src/app/api/leads/submit/route.ts</files>
  <read_first>the `claude-api` skill (SDK usage), apps/web/src/app/api/leads/submit/route.ts (after() block), apps/web/src/lib/ghl.ts (error-handling style)</read_first>
  <action>
Create `apps/web/src/lib/scoring.ts` (server-only; do not add 'use client'). Per the claude-api skill:
```
import Anthropic from '@anthropic-ai/sdk'

export interface LeadScoringInput {
  service_type: string
  message?: string | null
  source: string
  created_at: string
}

export interface UrgencyResult { score: number; reason: string }

const SYSTEM_PROMPT =
  'You are triaging inbound HVAC leads by urgency. Given the lead data, respond with ONLY a strict JSON object of the exact shape {"score": <integer 1-10>, "reason": "<short string>"} and nothing else. 10 = emergency (e.g. no heat/AC in extreme weather), 1 = low intent. Do not include markdown or prose.'

export async function scoreLeadUrgency(input: LeadScoringInput): Promise<UrgencyResult | null> {
  const apiKey = process.env.ANTHROPIC_API_KEY
  if (!apiKey) return null   // feature disabled — skip gracefully
  try {
    const client = new Anthropic({ apiKey })
    const resp = await client.messages.create(
      {
        model: 'claude-fable-5',
        max_tokens: 200,
        temperature: 0,
        system: SYSTEM_PROMPT,
        messages: [{ role: 'user', content: JSON.stringify(input) }],
      },
      { timeout: 8000 }
    )
    const block = resp.content.find((b) => b.type === 'text')
    const text = block && block.type === 'text' ? block.text : ''
    const match = text.match(/\{[\s\S]*\}/)   // defensive: extract first JSON object
    if (!match) return null
    const parsed = JSON.parse(match[0]) as { score?: unknown; reason?: unknown }
    const score = Math.max(1, Math.min(10, Math.round(Number(parsed.score))))
    if (!Number.isFinite(score)) return null
    const reason = typeof parsed.reason === 'string' ? parsed.reason.slice(0, 500) : ''
    return { score, reason }
  } catch (err) {
    console.error('[scoring] scoreLeadUrgency error:', err)
    return null
  }
}
```
In `apps/web/src/app/api/leads/submit/route.ts`, import `scoreLeadUrgency` and add a scoring block INSIDE the existing `after(async () => { ... })` (place it as its own try/catch, independent of the GHL and Resend blocks so a failure in one never affects the others):
```
try {
  const urgency = await scoreLeadUrgency({
    service_type,
    source: 'landing_page',
    created_at: new Date().toISOString(),
  })
  if (urgency) {
    await supabase
      .from('leads')
      .update({ urgency_score: urgency.score, urgency_reason: urgency.reason })
      .eq('id', lead.id)
  }
} catch (err) {
  console.error('[lead-submit] scoring error:', err)
}
```
Do not move the `return NextResponse.json(...)` — scoring must remain inside after(), after the 200 is returned.
  </action>
  <acceptance_criteria>
- `test -f apps/web/src/lib/scoring.ts && grep -q "claude-fable-5" apps/web/src/lib/scoring.ts`
- `grep -q "if (!apiKey) return null" apps/web/src/lib/scoring.ts` (graceful skip when unset).
- `grep -q "timeout: 8000" apps/web/src/lib/scoring.ts` (resilient — bounded call).
- `grep -q "scoreLeadUrgency" apps/web/src/app/api/leads/submit/route.ts`
- Scoring is inside after(): confirm the `scoreLeadUrgency(` call appears AFTER the `after(async () => {` line and BEFORE its closing — and the final `return NextResponse.json` remains the last statement of POST (unchanged).
- `cd apps/web && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && grep -q "scoreLeadUrgency" src/app/api/leads/submit/route.ts && echo OK</automated>
  </verify>
  <done>Form leads are scored via a resilient claude-fable-5 call in the background; failures/unset key never break lead capture or block the 200.</done>
</task>

<task type="auto">
  <name>Task 3: Sort hot leads first + Hot badge in the leads list</name>
  <files>apps/web/src/app/(dashboard)/dashboard/leads/page.tsx, apps/web/src/components/dashboard/leads-table.tsx</files>
  <read_first>apps/web/src/app/(dashboard)/dashboard/leads/page.tsx, apps/web/src/components/dashboard/leads-table.tsx (already has the 09-04 View link — preserve it), apps/web/src/components/ui/badge.tsx</read_first>
  <action>
In `apps/web/src/app/(dashboard)/dashboard/leads/page.tsx`, change the query ordering to put hot leads first, nulls last, then newest:
`.order('urgency_score', { ascending: false, nullsFirst: false }).order('created_at', { ascending: false })`.
In `apps/web/src/components/dashboard/leads-table.tsx`, in the Name cell, render a "Hot" `Badge` (variant `destructive`, from `@/components/ui/badge`) when `lead.urgency_score !== null && lead.urgency_score >= 8`. Keep the existing 09-04 "View" link and all columns. Optionally show the numeric score as a title/tooltip. Do not add 'use client' changes beyond what already exists (leads-table is already a client component).
  </action>
  <acceptance_criteria>
- `grep -q "urgency_score" apps/web/src/app/(dashboard)/dashboard/leads/page.tsx` and `grep -q "nullsFirst: false" apps/web/src/app/(dashboard)/dashboard/leads/page.tsx`
- `grep -q "urgency_score >= 8" apps/web/src/components/dashboard/leads-table.tsx`
- `grep -qi "Hot" apps/web/src/components/dashboard/leads-table.tsx`
- 09-04 link preserved: `grep -q "/dashboard/leads/" apps/web/src/components/dashboard/leads-table.tsx`
- `cd apps/web && npx tsc --noEmit && npm run build` both exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/user/Desktop/Tradeflow/apps/web && npx tsc --noEmit && npm run build && echo OK</automated>
  </verify>
  <done>The leads list orders by urgency (nulls last) and renders a Hot badge for scores >= 8, preserving existing columns and the lead-detail link; tsc + build pass.</done>
</task>

</tasks>

<verification>
- Migration 012 adds urgency_score/urgency_reason with a 1-10 check.
- `cd apps/web && npx tsc --noEmit && npm run build` exit 0.
- Scoring runs only inside after() and no-ops when ANTHROPIC_API_KEY is unset.
- Leads list sorts hot-first and shows the Hot badge.
</verification>

<success_criteria>
AI-01: inbound form leads receive a 1-10 urgency_score + reason via claude-fable-5, in the background, resilient and non-blocking. AI-02: the dashboard surfaces hot leads at the top with a Hot badge.
</success_criteria>

<output>
After completion, create `.planning/phases/09-features/09-05-SUMMARY.md`.
</output>
