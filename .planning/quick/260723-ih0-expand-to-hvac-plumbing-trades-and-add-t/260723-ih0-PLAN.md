---
phase: quick-260723-ih0
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [TRADE-DB, TRADE-CONFIG, TRADE-LANDING, TRADE-SCORING, TRADE-ADMIN, MKT-HOMEPAGE, MKT-SEO]
files_modified:
  - supabase/migrations/013_add_client_trade.sql
  - apps/web/src/lib/supabase/types.ts
  - apps/web/src/lib/trades.ts
  - apps/web/src/lib/validations/lead.ts
  - apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx
  - apps/web/src/app/(landing)/[clientSlug]/page.tsx
  - apps/web/src/components/lead-form.tsx
  - apps/web/src/lib/scoring.ts
  - apps/web/src/app/api/leads/submit/route.ts
  - apps/web/src/app/sitemap.ts
  - apps/web/src/lib/validations/onboarding.ts
  - apps/web/src/app/(admin)/admin/clients/new/page.tsx
  - apps/web/src/lib/actions/onboarding-actions.ts
  - apps/web/src/app/(marketing)/page.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/(marketing)/get-started/page.tsx
  - apps/web/src/app/(marketing)/privacy/page.tsx
autonomous: true

must_haves:
  truths:
    - "Existing HVAC landing page URLs (/{slug}/ac-repair etc.) still resolve unchanged"
    - "A plumbing client's landing pages resolve for drain-cleaning, water-heater-repair, leak-repair, emergency-plumbing"
    - "A single trade config module is the only source of truth for service slugs + labels"
    - "AI urgency scoring uses trade-appropriate prompt language (HVAC vs plumbing)"
    - "Admin can select HVAC or Plumbing when creating a client and it persists to clients.trade"
    - "Marketing homepage advertises HVAC + plumbing nationwide (no Chicagoland-only framing) and shows a Tech Services section"
    - "Frontend type-checks and builds cleanly"
  artifacts:
    - path: "supabase/migrations/013_add_client_trade.sql"
      provides: "clients.trade column, CHECK ('hvac','plumbing'), NOT NULL DEFAULT 'hvac'"
      contains: "add column"
    - path: "apps/web/src/lib/trades.ts"
      provides: "Trade config: per-trade service slugs, labels, default service, scoring copy; flat all-slugs export"
      min_lines: 40
    - path: "apps/web/src/lib/validations/lead.ts"
      provides: "service_type validation sourced from the flat trade slug list"
      contains: "trades"
  key_links:
    - from: "apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx"
      to: "apps/web/src/lib/trades.ts"
      via: "getTradeConfig(client.trade) for labels + service validation"
      pattern: "trades"
    - from: "apps/web/src/lib/scoring.ts"
      to: "apps/web/src/lib/trades.ts"
      via: "trade-parameterized system prompt"
      pattern: "trades|getTradeConfig"
    - from: "apps/web/src/lib/actions/onboarding-actions.ts"
      to: "clients.trade"
      via: "insert includes trade"
      pattern: "trade"
---

<objective>
Expand Tradeflow from HVAC-only to HVAC + plumbing, and reposition the marketing homepage for both trades nationwide with a new Tech Services section.

Purpose: Open the platform to plumbing contractors (and market tech-agency services) without breaking any existing HVAC client's landing pages, scoring, or dashboard.
Output: A `clients.trade` column, a single trade-config source of truth, trade-aware landing pages + lead form + AI scoring, a trade selector in admin onboarding, and a re-copied marketing site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor must preserve / extend. Use these directly. -->

Current hardcoded service list (apps/web/src/lib/validations/lead.ts):
```typescript
export const SERVICE_TYPES = ['ac-repair', 'furnace-repair', 'installation', 'maintenance'] as const
export const leadSchema = z.object({ ..., service_type: z.enum(SERVICE_TYPES, { error: 'Select a service type' }), ... })
export type LeadFormValues = z.input<typeof leadSchema>
```

Duplicated HVAC label maps that trades.ts must replace:
- apps/web/src/components/lead-form.tsx → `SERVICE_OPTION_LABELS` (ac-repair→'AC Repair', furnace-repair→'Furnace Repair', installation→'HVAC Installation', maintenance→'HVAC Maintenance')
- apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx → `SERVICE_LABELS` (same map)

Landing redirect stub currently hardcodes HVAC default (apps/web/src/app/(landing)/[clientSlug]/page.tsx):
```typescript
redirect(`/${clientSlug}/ac-repair`)
```

scoring.ts current signature (apps/web/src/lib/scoring.ts):
```typescript
export interface LeadScoringInput { service_type: string; message?: string | null; source: string; created_at: string }
export async function scoreLeadUrgency(input: LeadScoringInput): Promise<UrgencyResult | null>
// SYSTEM_PROMPT hardcodes: 'triaging inbound HVAC leads ... 10 = emergency (e.g. no heat/AC in extreme weather)'
```

scoring caller (apps/web/src/app/api/leads/submit/route.ts, inside after()):
```typescript
const urgency = await scoreLeadUrgency({ service_type, source: 'landing_page', created_at: new Date().toISOString() })
```

types.ts is a hand-written stub (apps/web/src/lib/supabase/types.ts) — clients Row/Insert/Update must each gain `trade`.

onboardClient insert (apps/web/src/lib/actions/onboarding-actions.ts) builds a clients insert object; formData is a plain typed object, validated by onboardingSchema.

Migration column-add style (supabase/migrations/012_add_urgency.sql) — lowercase `alter table public.X add column if not exists ...` + a named `add constraint ... check (...)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer + trade config source of truth</name>
  <files>supabase/migrations/013_add_client_trade.sql, apps/web/src/lib/supabase/types.ts, apps/web/src/lib/trades.ts, apps/web/src/lib/validations/lead.ts</files>
  <action>
Establish the DB column and the single trade-config module that every consumer will read.

1. **Migration** `supabase/migrations/013_add_client_trade.sql` (per decision #1). Match the lowercase style of 012:
   - Header comment explaining: adds `clients.trade`; all existing clients default to 'hvac' so existing landing pages are unaffected.
   - `alter table public.clients add column if not exists trade text not null default 'hvac';`
   - `alter table public.clients add constraint clients_trade_check check (trade in ('hvac','plumbing'));`
   (Adding a NOT NULL column with a DEFAULT backfills existing rows to 'hvac' automatically, satisfying the CHECK.)

2. **Types stub** `apps/web/src/lib/supabase/types.ts` — add `trade: string` to the clients `Row`, `Insert` (as `trade?: string`, since DB has a default), and `Update` (as `trade?: string`). Place it next to the other client columns (e.g. after `slug` / near `notifications_enabled`). Do not touch other tables.

3. **NEW config module** `apps/web/src/lib/trades.ts` (per decision #2) — the single source of truth. Export:
   - `export type Trade = 'hvac' | 'plumbing'`
   - `export interface ServiceDef { slug: string; label: string }`
   - `export interface TradeConfig { trade: Trade; label: string; defaultService: string; services: ServiceDef[]; scoringDomain: string; scoringEmergencyExamples: string }`
   - `export const TRADES: Record<Trade, TradeConfig>` with:
     - hvac → label 'HVAC', defaultService 'ac-repair', services [ac-repair→'AC Repair', furnace-repair→'Furnace Repair', installation→'HVAC Installation', maintenance→'HVAC Maintenance'], scoringDomain 'HVAC', scoringEmergencyExamples 'no heat or AC in extreme weather'
     - plumbing → label 'Plumbing', defaultService 'emergency-plumbing', services [drain-cleaning→'Drain Cleaning', water-heater-repair→'Water Heater Repair', leak-repair→'Leak Repair', emergency-plumbing→'Emergency Plumbing'], scoringDomain 'plumbing', scoringEmergencyExamples 'a burst pipe, active flooding, or no hot water'
   - `export function getTradeConfig(trade: string | null | undefined): TradeConfig` → returns `TRADES[trade as Trade] ?? TRADES.hvac` (default 'hvac' when unknown — keep every existing/legacy client working).
   - `export function getServiceLabel(trade: string | null | undefined, slug: string): string` → look up in that trade's services, fall back to the raw slug.
   - `export function isValidServiceForTrade(trade: string | null | undefined, slug: string): boolean`.
   - `export const ALL_SERVICE_SLUGS` → a flat `as const`-friendly readonly tuple of every slug across both trades (hvac slugs first, then plumbing), for callers that validate ANY service slug. Build it so it's a non-empty tuple usable by `z.enum` (e.g. `export const ALL_SERVICE_SLUGS = ['ac-repair','furnace-repair','installation','maintenance','drain-cleaning','water-heater-repair','leak-repair','emergency-plumbing'] as const`). Add a comment that this array must stay in sync with TRADES.
   This module is pure data (no 'server-only', no imports) so both server and client components may import it.

4. **Lead validation** `apps/web/src/lib/validations/lead.ts` (per decision #2) — replace the hardcoded array:
   - `import { ALL_SERVICE_SLUGS } from '@/lib/trades'`
   - `export const SERVICE_TYPES = ALL_SERVICE_SLUGS` (keep the named export so existing importers compile) — or re-export directly.
   - Change the schema field to `service_type: z.enum(ALL_SERVICE_SLUGS, { error: 'Select a service type' })`.
   - Keep `LeadFormValues` export unchanged in shape.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Migration 013 exists with the trade column + CHECK; types.ts clients Row/Insert/Update include `trade`; trades.ts exports TRADES + helpers + ALL_SERVICE_SLUGS; lead.ts validates against the flat slug list; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Trade-aware landing pages, lead form, AI scoring, sitemap</name>
  <files>apps/web/src/app/(landing)/[clientSlug]/[service]/page.tsx, apps/web/src/app/(landing)/[clientSlug]/page.tsx, apps/web/src/components/lead-form.tsx, apps/web/src/lib/scoring.ts, apps/web/src/app/api/leads/submit/route.ts, apps/web/src/app/sitemap.ts</files>
  <action>
Wire every public-facing consumer to the trade config from Task 1 (decisions #3 and #4). Preserve existing HVAC URLs and the sub-2s static-generation pattern.

1. **Landing service page** `[clientSlug]/[service]/page.tsx`:
   - Delete the local `SERVICE_LABELS` map; import `getTradeConfig, getServiceLabel, isValidServiceForTrade` from `@/lib/trades`.
   - `generateStaticParams`: select `slug, trade` from clients (was `slug` only); cross each client with `getTradeConfig(client.trade).services` slugs (was the global `SERVICE_TYPES`). Keep the existing placeholder-URL guard and empty-array fallback untouched.
   - `generateMetadata`: fetch `business_name, city, trade`; validate the service against the client's trade via `isValidServiceForTrade(client.trade, service)` (instead of the global `SERVICE_TYPES.includes`); compute the label with `getServiceLabel(client.trade, service)`. Note: metadata currently returns early `{}` before fetching the client — reorder so the trade is known before validating (fetch client first, then validate service against its trade; return a neutral title if the client is missing).
   - Page component: add `trade` to the client `select`; validate the service against the client's trade (`isValidServiceForTrade`) → `notFound()` when invalid; compute `serviceLabel` with `getServiceLabel(client.trade, service)`. Pass `trade={client.trade as string}` to `<LeadForm>` (in addition to the existing props). Copy that says "HVAC" generically in trust blocks can stay as-is EXCEPT the page title/label which now come from the trade config; do not hardcode HVAC-specific service words that a plumbing page would render wrong.
   - Keep `export const revalidate = 3600`.

2. **Landing redirect stub** `[clientSlug]/page.tsx` (decision #3): it currently redirects to `/${clientSlug}/ac-repair` unconditionally. Change it to fetch the client's `trade` (service-role `createClient` from `@supabase/supabase-js`, same guarded pattern as the sibling page / sitemap) and redirect to `/${clientSlug}/${getTradeConfig(trade).defaultService}` (hvac → ac-repair, plumbing → emergency-plumbing). If the client can't be fetched (missing env / not found), fall back to the hvac default so behavior never regresses. Keep `export const revalidate = 3600`.

3. **Lead form** `apps/web/src/components/lead-form.tsx` (decision #3): remove the local `SERVICE_OPTION_LABELS` map and the `SERVICE_TYPES` import for rendering options. Add a `trade: string` prop to `LeadFormProps`. Derive the option list from `getTradeConfig(trade).services` and render `<option value={s.slug}>{s.label}</option>`. Keep the `serviceType` prop (used as the pre-selected default) and everything else unchanged.

4. **AI scoring** `apps/web/src/lib/scoring.ts` (decision #4): add `trade?: string` to `LeadScoringInput`. Build the system prompt dynamically from `getTradeConfig(input.trade)`: e.g. `You are triaging inbound ${cfg.scoringDomain} leads by urgency. ... 10 = emergency (e.g. ${cfg.scoringEmergencyExamples}), 1 = low intent. ...`. Default to hvac when trade is unset (getTradeConfig already does this). Do not send `trade` in the JSON payload passed to the model unless helpful — the prompt language is what matters. Keep all the resilience behavior (null on missing key / parse failure / timeout) intact.

5. **Lead submit route** `apps/web/src/app/api/leads/submit/route.ts` (decision #4): inside the AI scoring `try` block in `after()`, fetch the client's trade before scoring — e.g. `const { data: c } = await supabase.from('clients').select('trade').eq('id', client_id).single()` — and pass `trade: c?.trade ?? undefined` into `scoreLeadUrgency({ ... })`. Keep this as its own isolated try/catch (a scoring/trade-fetch failure must never affect GHL, Resend, or the already-returned 200).

6. **Sitemap** `apps/web/src/app/sitemap.ts` (decision #3): select `slug, trade` from clients; import `getTradeConfig` from `@/lib/trades` (drop the global `SERVICE_TYPES` import); cross each client with `getTradeConfig(client.trade).services` slugs. Keep the placeholder-env guard and the try/catch degrade-to-static behavior.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>Landing pages fetch + honor `trade` for static params, metadata, validation, labels, and the redirect default; LeadForm renders the trade's services; scoring prompt is trade-parameterized and the submit route passes the client's trade; sitemap crosses each client × its trade's services; existing HVAC slugs still resolve; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 3: Admin trade selector + marketing repositioning + Tech Services + SEO copy</name>
  <files>apps/web/src/lib/validations/onboarding.ts, apps/web/src/app/(admin)/admin/clients/new/page.tsx, apps/web/src/lib/actions/onboarding-actions.ts, apps/web/src/app/(marketing)/page.tsx, apps/web/src/app/layout.tsx, apps/web/src/app/(marketing)/get-started/page.tsx, apps/web/src/app/(marketing)/privacy/page.tsx</files>
  <action>
Add the admin trade selector (decision #5), reposition the marketing site for HVAC + plumbing nationwide with a Tech Services section (decision #6), and fix remaining SEO/metadata + copy (decision #7).

1. **Onboarding validation** `apps/web/src/lib/validations/onboarding.ts` (decision #5): add `trade: z.enum(['hvac', 'plumbing'])` to `onboardingSchema`. Do NOT use `.default()` (per the Phase 04 note in STATE.md, `.default()` makes the RHF input type optional and breaks the Resolver type — set the default in the form's `defaultValues` instead).

2. **Admin new-client form** `apps/web/src/app/(admin)/admin/clients/new/page.tsx` (decision #5): add `trade: 'hvac'` to `defaultValues`. Add a Trade `<select>` (mirror the existing Plan select styling: `className="w-full rounded-md border border-gray-300 px-3 py-2 text-sm"`) with options HVAC (`hvac`) and Plumbing (`plumbing`), wired via `{...form.register('trade')}`, with the same label + error-message pattern as the other fields. Place it near the top (e.g. right after Business Name or beside Plan). Update the trade-specific placeholders to be neutral: `business_name` placeholder `"Oak Park HVAC"` → `"Acme Home Services"` (or similar neutral value); leave owner/city placeholders as-is.

3. **Onboarding action** `apps/web/src/lib/actions/onboarding-actions.ts` (decision #5): add `trade: string` to the `formData` param type, and include `trade: parsed.data.trade` in the `.insert({...})` object for the clients row. No other logic changes.

4. **Marketing homepage** `apps/web/src/app/(marketing)/page.tsx` (decision #6) — drop ALL Chicagoland-only framing and add a Tech Services section:
   - Hero badge: `Built for Chicagoland HVAC` → `Built for HVAC & plumbing companies`.
   - Pain-points subhead: `Most HVAC companies lose 40–60%...` → `Most home service companies lose 40–60%...`.
   - Google LSA feature list item `Target specific zip codes across Chicagoland` → nationwide phrasing, e.g. `Target specific zip codes anywhere in the U.S.`.
   - SMS sequences copy: the line `Different messages for AC repair, furnace installs, and maintenance` → mention both trades' jobs, e.g. `Different messages for AC repair, furnace installs, drain cleaning, and water-heater jobs` (and adjust the "Different flows for each service type" item only if needed — it's already trade-neutral).
   - Final CTA paragraph: `We're onboarding a small number of HVAC contractors in Chicagoland right now.` → `We're onboarding HVAC & plumbing companies across the United States right now.`
   - Footer tagline `AI-powered lead generation for HVAC contractors` → `AI-powered lead generation for HVAC & plumbing companies`.
   - Nav: add a `Tech services` anchor link (`href="#tech-services"`) alongside the existing How it works / Pricing links, matching their exact classes.
   - NEW `#tech-services` section (place it right AFTER the Pricing section, before the Final CTA) — visually consistent with the existing dark theme + gold (#D4AF37) accents and the existing section shell (reuse `GoldBadge` and the same heading font `'Gambetta', Georgia, serif` + spacing used by other sections). Content: a `GoldBadge` (e.g. "Tech Services"), a heading (e.g. "Need more than leads? We build the tech too."), a short subhead, and a responsive grid (`grid-cols-1 md:grid-cols-2` or `md:grid-cols-4`, matching the card styling used in Pain Points / Pricing: `rounded-2xl p-6/7`, `background: rgba(255,255,255,0.02)`, `border: 1px solid rgba(255,255,255,0.06)`) advertising four services: **Custom websites & landing pages**, **AI automation & chatbots**, **Custom software & apps**, **CRM & integrations setup** — each with a short one-line description. No prices. A single CTA "Contact us — book a call" as a mailto link to `hello@tradeflow-technologies.com` (reuse the existing gold button styling; you may reuse the existing mailto subject pattern seen in the Final CTA / footer). Keep it accessible (focus-visible ring classes consistent with other links/buttons).

5. **Global metadata** `apps/web/src/app/layout.tsx` (decision #7): update `title` and `description` to cover HVAC + plumbing and mention tech services, e.g. title `Tradeflow — AI-powered lead generation for HVAC & plumbing companies`, description something like `Tradeflow captures every call, form, and missed contact — then follows up automatically so home service companies never lose a lead. We also build custom websites, AI automation, and software.` Keep `metadataBase` unchanged.

6. **Get-started page** `apps/web/src/app/(marketing)/get-started/page.tsx` (decision #7): badge `Now onboarding Chicagoland HVAC` → nationwide HVAC & plumbing (e.g. `Now onboarding HVAC & plumbing — nationwide`); footer tagline `AI-powered lead generation for HVAC contractors` → `AI-powered lead generation for HVAC & plumbing companies`. The `Smith HVAC` placeholder may stay. Leave form logic untouched.

7. **Privacy page** `apps/web/src/app/(marketing)/privacy/page.tsx` (decision #7): metadata `description` — `...homeowners and HVAC contractor clients.` → `...homeowners and home service clients.` No body changes required (body already says "home service businesses").

Do NOT touch the FastAPI backend (the api/models/schemas.py HVAC comment cleanup is explicitly optional and out of scope for this plan).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Admin onboarding has a working HVAC/Plumbing selector that persists to clients.trade; the marketing homepage has no Chicagoland-only framing, advertises both trades nationwide, and shows a Tech Services section with a nav anchor + mailto CTA; global/get-started/privacy copy + metadata updated; `npx tsc --noEmit` and `npm run build` both succeed.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes (no type errors from the new `trade` column, trades.ts, or prop changes).
- `cd apps/web && npm run build` completes cleanly (generateStaticParams/sitemap guards keep the build green without live Supabase env).
- Grep sanity: no remaining `Chicagoland` in `apps/web/src/app/(marketing)/page.tsx`; no local `SERVICE_LABELS` / `SERVICE_OPTION_LABELS` maps left in the landing page or lead-form (they now import from `@/lib/trades`).
- Existing HVAC slugs unchanged: `ac-repair`, `furnace-repair`, `installation`, `maintenance` still appear in `trades.ts` and still generate the same URLs.
</verification>

<success_criteria>
- Migration 013 adds `clients.trade` (text, CHECK hvac|plumbing, NOT NULL DEFAULT 'hvac') in the style of 001–012.
- `apps/web/src/lib/trades.ts` is the single source of truth; `lead.ts`, both landing files, `lead-form.tsx`, `scoring.ts`, and `sitemap.ts` all read from it.
- Plumbing clients get the four plumbing slugs; HVAC clients are unchanged and every existing HVAC URL still resolves.
- AI scoring, admin onboarding, and the client insert are all trade-aware.
- Marketing site repositioned for HVAC + plumbing nationwide with a Tech Services section (mailto CTA, no public pricing) and updated SEO/copy across layout, get-started, and privacy.
- Frontend type-checks and builds cleanly.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-ih0-expand-to-hvac-plumbing-trades-and-add-t/260723-ih0-SUMMARY.md`
</output>
