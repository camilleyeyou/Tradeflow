---
phase: quick-260723-ph8
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DEMO-SEED, JOB-VALUE, CLIENT-TZ]
files_modified:
  - supabase/migrations/015_job_value_and_timezone.sql
  - supabase/seed_demo.sql
  - apps/web/src/lib/supabase/types.ts
  - apps/web/src/lib/types/dashboard.ts
  - apps/web/src/lib/timezones.ts
  - apps/web/src/lib/utils/format.ts
  - apps/web/src/lib/actions/lead-actions.ts
  - apps/web/src/lib/actions/onboarding-actions.ts
  - apps/web/src/lib/actions/settings-actions.ts
  - apps/web/src/lib/validations/settings.ts
  - apps/web/src/lib/validations/onboarding.ts
  - apps/web/src/lib/ghl.ts
  - apps/web/src/components/dashboard/job-value-editor.tsx
  - apps/web/src/components/dashboard/roi-summary.tsx
  - apps/web/src/components/dashboard/leads-table.tsx
  - apps/web/src/components/dashboard/recent-leads.tsx
  - apps/web/src/components/dashboard/calls-table.tsx
  - apps/web/src/components/dashboard/sms-inbox.tsx
  - apps/web/src/components/dashboard/settings-form.tsx
  - apps/web/src/app/(dashboard)/dashboard/page.tsx
  - apps/web/src/app/(dashboard)/dashboard/leads/page.tsx
  - apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx
  - apps/web/src/app/(dashboard)/dashboard/calls/page.tsx
  - apps/web/src/app/(admin)/admin/clients/new/page.tsx
  - apps/web/src/app/api/cron/monthly-reports/route.ts

must_haves:
  truths:
    - "An owner can enter a job value (in dollars) on any lead and it persists as leads.job_value_cents"
    - "Dashboard ROI card shows real reported value + estimated remainder with honest labeling"
    - "Monthly report email splits reported vs estimated value, labeled"
    - "Each client's leads/calls/messages render timestamps in that client's own timezone"
    - "Admin picks a timezone at onboarding; owner can change it in settings; GHL sub-account is created with it"
    - "seed_demo.sql produces an is_active=false demo client that is excluded from landing pages, sitemap, and monthly reports"
  artifacts:
    - path: "supabase/migrations/015_job_value_and_timezone.sql"
      provides: "leads.job_value_cents + clients.timezone + column grant"
      contains: "job_value_cents"
    - path: "supabase/seed_demo.sql"
      provides: "Standalone re-runnable demo client + leads + calls + sms"
      contains: "demo-summit-air-plumbing"
    - path: "apps/web/src/lib/timezones.ts"
      provides: "Curated US timezone list + enum values"
    - path: "apps/web/src/components/dashboard/job-value-editor.tsx"
      provides: "Client component to edit a lead's job value"
    - path: "apps/web/src/lib/utils/format.ts"
      provides: "formatClientTime(iso, timeZone) with Chicago fallback"
      contains: "formatClientTime"
  key_links:
    - from: "components/dashboard/job-value-editor.tsx"
      to: "lib/actions/lead-actions.ts updateLeadJobValue"
      via: "server action call"
      pattern: "updateLeadJobValue"
    - from: "app/(dashboard)/dashboard/page.tsx"
      to: "components/dashboard/roi-summary.tsx"
      via: "reported + estimated props derived from job_value_cents"
      pattern: "job_value_cents"
    - from: "lib/actions/onboarding-actions.ts"
      to: "lib/ghl.ts createSubAccount"
      via: "timezone threaded into sub-account creation"
      pattern: "timezone"
---

<objective>
Ship three locked, user-approved features in one plan: (1) a demo client seed for sales calls, (2) real job-value tracking on completed leads, and (3) per-client timezone support. One idempotent migration (015) backs the two schema-touching features; the demo seed is a standalone hand-run SQL file (NOT a migration).

Purpose: Give sales a realistic demo dashboard, replace the pure $400/lead estimate with actual reported job revenue where available, and make timestamps correct for clients outside Chicago.
Output: Migration 015, seed_demo.sql, a job-value editor + action, real+estimated ROI math on the dashboard and monthly report, a shared timezone list, a generalized time formatter threaded through every dashboard surface, and timezone selects on both the settings and admin-onboarding forms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md

# Migration + seed style
@supabase/migrations/014_review_requests_and_reports.sql
@supabase/migrations/012_add_urgency.sql
@supabase/seed.sql

# Job-value surfaces
@apps/web/src/components/dashboard/roi-summary.tsx
@apps/web/src/lib/actions/lead-actions.ts
@apps/web/src/components/dashboard/notes-editor.tsx
@apps/web/src/app/(dashboard)/dashboard/page.tsx
@apps/web/src/app/api/cron/monthly-reports/route.ts

# Timezone surfaces
@apps/web/src/lib/utils/format.ts
@apps/web/src/lib/validations/settings.ts
@apps/web/src/lib/validations/onboarding.ts
@apps/web/src/components/dashboard/settings-form.tsx
@apps/web/src/lib/actions/onboarding-actions.ts
@apps/web/src/lib/ghl.ts

# Types
@apps/web/src/lib/supabase/types.ts
@apps/web/src/lib/types/dashboard.ts

<interfaces>
<!-- Key contracts the executor needs. Use these directly. -->

leads schema (migration 001) has: id, client_id, homeowner_name, phone, zip_code,
service_type, source, status ('new'|'contacted'|'booked'|'completed'|'lost'),
notes, ghl_contact_id, created_at, first_contact_at, urgency_score, urgency_reason.

clients schema has: id, business_name, owner_name, email, phone, city, state,
service_area_zips text[], plan, is_active, notifications_enabled, slug, trade,
review_rating, review_count, google_review_url, review_requests_enabled.

hvac service slugs (lib/trades.ts): 'ac-repair','furnace-repair','installation','maintenance'.

is_active=true filter CONFIRMED in all three exclusion points:
- app/(landing)/[clientSlug]/page.tsx:29 and [clientSlug]/[service]/page.tsx (incl. generateStaticParams)
- app/sitemap.ts:29
- app/api/cron/monthly-reports/route.ts:57
So is_active=false keeps the demo client out of landing pages, sitemap, and reports.

Existing dashboard pages fetch the client's timezone via the client_users→clients
embed (same shape the layout/settings pages already use):
  supabase.from('client_users').select('clients(timezone)').eq('user_id', user.id).single()
  -> { clients: { timezone } | null }

Zod is v4 (z.enum accepts readonly tuples — no cast needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 015, type stubs, and standalone demo seed</name>
  <files>supabase/migrations/015_job_value_and_timezone.sql, apps/web/src/lib/supabase/types.ts, apps/web/src/lib/types/dashboard.ts, supabase/seed_demo.sql</files>
  <action>
1. Create `supabase/migrations/015_job_value_and_timezone.sql` (idempotent, style like 014):
   - `alter table public.leads add column if not exists job_value_cents integer;`
   - Guard the CHECK so re-runs don't error (012 uses a bare `add constraint` which is NOT re-runnable — do NOT copy that; use a DO block):
     ```sql
     do $$
     begin
       if not exists (select 1 from pg_constraint where conname = 'leads_job_value_cents_check') then
         alter table public.leads
           add constraint leads_job_value_cents_check
           check (job_value_cents is null or job_value_cents >= 0);
       end if;
     end $$;
     ```
   - `alter table public.clients add column if not exists timezone text not null default 'America/Chicago';`
   - `grant update (timezone) on public.clients to authenticated;` (extends the migration-008 column-scoped grant so owners can set it from settings)

2. `apps/web/src/lib/supabase/types.ts` (hand-written stub): add `timezone: string` to clients Row, `timezone?: string` to clients Insert and Update; add `job_value_cents: number | null` to leads Row, and `job_value_cents?: number | null` to leads Insert and Update.

3. `apps/web/src/lib/types/dashboard.ts`: add `timezone: string` to the `Client` interface and `job_value_cents: number | null` to the `Lead` interface.

4. Create `supabase/seed_demo.sql` — a STANDALONE hand-run file (NOT a migration). Comment header must explain: apply all migrations INCLUDING 015 first, then run this whole file in the Supabase SQL editor, then use the admin "create login" button on the demo client to provision a demo dashboard login. Requirements:
   - Re-runnable: first `delete from public.clients where slug = 'demo-summit-air-plumbing';` (cascades to leads → sms_sequences, and to calls).
   - One demo client with a FIXED id (e.g. `'d0000000-0000-0000-0000-000000000001'`): business_name `'Summit Air & Plumbing (Demo)'`, owner_name `'Demo Owner'`, trade `'hvac'`, city `'Chicago'`, state `'IL'`, phone `'(555) 010-0100'`, email `'demo@tradeflow-technologies.com'`, service_area_zips e.g. `ARRAY['60601','60614','60622']`, slug `'demo-summit-air-plumbing'`, plan `'growth'`, `is_active = false` (CRITICAL — the three exclusion filters above depend on it; add a one-line SQL comment noting this), `notifications_enabled = false`, `review_rating = 4.8`, `review_count = 47`, `google_review_url = 'https://g.page/r/demo-example/review'`, `timezone = 'America/Chicago'`.
   - ~24 leads over the last 30 days using `now() - interval 'N days'` (and minutes) so it always looks fresh. Give ~8 leads FIXED ids (`'d0000000-0000-0000-0000-0000000000NN'`) so calls/sms can reference them; the rest use the default id. Columns to populate: client_id (demo id), homeowner_name (realistic first name + last initial, e.g. `'Marcus T.'`), phone (fake 555 numbers like `'(555) 010-0xxx'`), zip_code (from the service_area_zips), service_type (mixed hvac slugs: ac-repair/furnace-repair/installation/maintenance), source (mixed: 'landing_form','direct_call','google_lsa'), status (distributed across new/contacted/booked/completed/lost), created_at (`now() - interval '...'`), first_contact_at (`created_at + interval '4 minutes'`-ish, only for contacted/booked/completed/lost — leave NULL for 'new'), urgency_score (1–10; set urgency_reason strings only on the high 8–10 ones, e.g. `'No heat, elderly resident'`), job_value_cents on ~6 of the completed leads in the 38000–420000 range ($380–$4,200), NULL elsewhere.
   - ~14 calls rows: client_id (demo id), lead_id (link ~half to the fixed-id leads, NULL for the rest), caller_number (fake 555), tracking_number `'(555) 010-0199'`, duration_seconds (realistic: 0 for missed, 20–90 for voicemail, 60–600 for answered), outcome (mix of 'answered'/'missed'/'voicemail'), recording_url NULL, called_at (`now() - interval '...'`).
   - A few (3–5) sms_sequences rows referencing fixed-id leads: touch_number, message_body (realistic follow-up copy), status (mix of 'pending'/'sent'), scheduled_at, sent_at (set only when status='sent').
   - Every phone/number MUST be an obviously-fake 555 number; nothing may read as a real testimonial or real business.
  </action>
  <verify>
    <automated>test -f supabase/migrations/015_job_value_and_timezone.sql && test -f supabase/seed_demo.sql && grep -q "job_value_cents" apps/web/src/lib/supabase/types.ts && grep -q "timezone" apps/web/src/lib/types/dashboard.ts && grep -q "is_active" supabase/seed_demo.sql && echo OK</automated>
  </verify>
  <done>Migration 015 is idempotent (guarded CHECK, if-not-exists column, grant); type stubs expose job_value_cents and timezone; seed_demo.sql is a standalone delete-then-insert file with an is_active=false demo client, ~24 leads, ~14 calls, and a few sms rows, all using fake 555 data.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Real job-value tracking (action, editor, ROI card, monthly report, leads table)</name>
  <files>apps/web/src/lib/actions/lead-actions.ts, apps/web/src/components/dashboard/job-value-editor.tsx, apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx, apps/web/src/components/dashboard/roi-summary.tsx, apps/web/src/app/(dashboard)/dashboard/page.tsx, apps/web/src/components/dashboard/leads-table.tsx, apps/web/src/app/api/cron/monthly-reports/route.ts</files>
  <action>
1. `lib/actions/lead-actions.ts`: add `export async function updateLeadJobValue(leadId: string, jobValueCents: number | null)`, following the existing `updateLeadNotes` shape (auth via `supabase.auth.getUser()`, RLS-scoped update, no manual client_id check). Validation: if not null, must be a finite integer, `>= 0`, and `<= 100_000_000` (cap = $1,000,000 → 100,000,000 cents); throw on violation. Empty/null clears the column to NULL. On success `revalidatePath('/dashboard/leads')`, `revalidatePath('/dashboard')`, and `revalidatePath(\`/dashboard/leads/\${leadId}\`)`.

2. Create `components/dashboard/job-value-editor.tsx` (client component, model on notes-editor.tsx): props `{ leadId: string; initialCents: number | null; status: LeadStatus }`. Show a dollar-denominated number input (value shown as dollars = cents/100). On save, convert dollars→cents with `Math.round(dollars * 100)`, empty string → null, then call `updateLeadJobValue` inside `startTransition`. Render it prominently (label "Job value", larger) when `status === 'completed'`, and as a compact editable field otherwise (it must be visible for any status). Reject negative/over-cap in the UI too (mirror the action's bounds) before calling the action.

3. `app/(dashboard)/dashboard/leads/[id]/page.tsx`: render `<JobValueEditor leadId={typedLead.id} initialCents={typedLead.job_value_cents} status={typedLead.status} />` in the detail grid/card (the page already selects `*`, so job_value_cents is present). (Timezone threading for this page is done in Task 3 — do not duplicate.)

4. `components/dashboard/roi-summary.tsx`: change `RoiSummaryProps` to `{ monthLeadCount: number; reportedValueDollars: number; estimatedValueDollars: number; avgSpeedToLeadMinutes: number | null }`. Keep `ESTIMATED_LEAD_VALUE` exported (the cron route imports it). In the value card compute `total = reportedValueDollars + estimatedValueDollars` and render honestly:
   - reported > 0 and estimated > 0 → show `$total`, subtitle `"$X reported + $Y estimated"`.
   - reported > 0 and estimated === 0 → show `$reported`, subtitle `"Reported job value"`.
   - reported === 0 → show `$estimated`, keep the existing subtitle `"Estimated at $${ESTIMATED_LEAD_VALUE}/lead — not actual revenue"`.

5. `app/(dashboard)/dashboard/page.tsx`: add `job_value_cents` to the leads `.select(...)`. Derive from the month's COMPLETED leads: `reportedCents = sum(job_value_cents ?? 0)` over completed leads that have a value; `completedWithoutValue = count of completed leads with job_value_cents == null`; `reportedValueDollars = Math.round(reportedCents / 100)`; `estimatedValueDollars = completedWithoutValue * ESTIMATED_LEAD_VALUE`. Pass the two new props (drop the old `estimatedValue`).

6. `app/api/cron/monthly-reports/route.ts`: add `job_value_cents: number | null` to `LeadStatsRow` and to the leads `.select(...)`. Replace `estimatedValue = totalLeads * ESTIMATED_LEAD_VALUE` with the same completed-leads split (reportedValue dollars + estimatedValue dollars over completed leads only); render the "Estimated pipeline value" row with an honest label, e.g. `$${(reportedValue + estimatedValue).toLocaleString()}` followed by `(${reportedValue} reported + ${estimatedValue} estimated)` when reportedValue > 0, else the existing "(estimated — not actual revenue)" note.

7. `components/dashboard/leads-table.tsx`: add a `Value` column as `hidden lg:table-cell` (mobile/tablet hidden so it never crowds) showing `$` + `(lead.job_value_cents/100).toLocaleString()` when the lead is completed AND has a value, else `—`. Add the matching `<TableHead className="hidden lg:table-cell">Value</TableHead>`.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>updateLeadJobValue validates and clears correctly; the lead detail page has a job-value input (prominent when completed); the dashboard ROI card and monthly report both show real reported + estimated split with honest labels; the leads table shows a value column for completed leads on large screens only; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Per-client timezone (formatter, shared list, forms/validation, ghl, thread through all surfaces)</name>
  <files>apps/web/src/lib/timezones.ts, apps/web/src/lib/utils/format.ts, apps/web/src/lib/validations/settings.ts, apps/web/src/lib/validations/onboarding.ts, apps/web/src/lib/actions/settings-actions.ts, apps/web/src/components/dashboard/settings-form.tsx, apps/web/src/app/(admin)/admin/clients/new/page.tsx, apps/web/src/lib/actions/onboarding-actions.ts, apps/web/src/lib/ghl.ts, apps/web/src/components/dashboard/leads-table.tsx, apps/web/src/components/dashboard/recent-leads.tsx, apps/web/src/components/dashboard/calls-table.tsx, apps/web/src/components/dashboard/sms-inbox.tsx, apps/web/src/app/(dashboard)/dashboard/page.tsx, apps/web/src/app/(dashboard)/dashboard/leads/page.tsx, apps/web/src/app/(dashboard)/dashboard/leads/[id]/page.tsx, apps/web/src/app/(dashboard)/dashboard/calls/page.tsx</files>
  <action>
1. Create `lib/timezones.ts` (pure data module — no imports, no 'server-only', like trades.ts): export `US_TIMEZONES` (curated: America/New_York=ET, America/Chicago=CT, America/Denver=MT, America/Phoenix=AZ, America/Los_Angeles=PT, America/Anchorage=AK, Pacific/Honolulu=HI — labels like `'Eastern (ET)'`, `'Central (CT)'`, etc.) as `{ value, label }[]`; export a `TIMEZONE_VALUES` literal tuple (`as const`) of the 7 values; export `DEFAULT_TIMEZONE = 'America/Chicago'`.

2. `lib/utils/format.ts`: add `formatClientTime(isoString: string, timeZone?: string | null): string` using the same `toLocaleString('en-US', { month:'short', day:'numeric', hour:'numeric', minute:'2-digit' })` fields but with `timeZone: timeZone || 'America/Chicago'`. Wrap in try/catch — an invalid IANA tz throws a RangeError, so on catch re-format with `'America/Chicago'`. Keep `formatChicagoTime` as a thin deprecated wrapper: `return formatClientTime(isoString, 'America/Chicago')` (add a `@deprecated` JSDoc line). Do NOT delete formatChicagoTime — leave existing importers working until they're all migrated below.

3. `lib/validations/settings.ts`: add `timezone: z.enum(TIMEZONE_VALUES)` (import from '@/lib/timezones'). `lib/validations/onboarding.ts`: add `timezone: z.enum(TIMEZONE_VALUES)` (no `.default()` — set the default in the form's defaultValues instead, per the Phase-04 resolver-type note).

4. `lib/actions/settings-actions.ts`: add `timezone: string` to the `updateClientSettings` formData param type. No other change needed — it already `settingsSchema.safeParse`s and `.update(parsed.data)`s, so the granted `timezone` column flows through.

5. `components/dashboard/settings-form.tsx`: add `timezone: client.timezone ?? 'America/Chicago'` to defaultValues and a native `<select {...register('timezone')}>` (mapping `US_TIMEZONES`) inside the Business Information card, styled to match the new-client page's selects. Show `errors.timezone.message` if present.

6. `app/(admin)/admin/clients/new/page.tsx`: add `timezone: 'America/Chicago'` to defaultValues and a native `<select {...form.register('timezone')}>` over `US_TIMEZONES` (place near State/Plan), with the same error display pattern.

7. `lib/actions/onboarding-actions.ts`: add `timezone: string` to the formData param type; include `timezone: parsed.data.timezone` in the clients `.insert(...)`; and pass `timezone: parsed.data.timezone` into the `createSubAccount({ ... })` call.

8. `lib/ghl.ts`: add `timezone: string` to `CreateSubAccountInput` and use `timezone: input.timezone` in the `/locations/` POST body (replace the hardcoded `'America/Chicago'`).

9. Thread the client timezone into every display surface. In each of the four dashboard pages below, fetch the timezone once (RLS-safe embed):
   ```ts
   const { data: cu } = await supabase.from('client_users').select('clients(timezone)').eq('user_id', user.id).single()
   const timeZone = (cu?.clients as { timezone: string } | null)?.timezone ?? 'America/Chicago'
   ```
   - `dashboard/leads/page.tsx` → pass `timeZone` to `<LeadsTable />`.
   - `dashboard/page.tsx` → pass `timeZone` to `<RecentLeads />`.
   - `dashboard/calls/page.tsx` → pass `timeZone` to `<CallsTable />`.
   - `dashboard/leads/[id]/page.tsx` → use `formatClientTime(typedLead.created_at, timeZone)` for Created, and pass `timeZone` to `<SmsInbox />`.
   Then update each component to accept a `timeZone: string` prop and replace `formatChicagoTime(x)` with `formatClientTime(x, timeZone)`:
   - `leads-table.tsx` (LeadsTableProps + the Created cell)
   - `recent-leads.tsx` (RecentLeadsProps + the Date cell)
   - `calls-table.tsx` (CallsTableProps + the Date cell)
   - `sms-inbox.tsx` (SmsInboxProps + the message timestamp)
   (Pre-migration the `clients(timezone)` select would error, but per the locked deploy-order rule migration 015 is applied before this code deploys — acceptable, same rule as prior columns.)
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>formatClientTime exists with a Chicago fallback and formatChicagoTime delegates to it; a curated timezone select appears in both the settings and admin-onboarding forms and validates via zod; onboarding writes clients.timezone and creates the GHL sub-account with it; all dashboard leads/calls/messages render in the client's timezone; tsc and the production build pass.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes (types.ts stub + dashboard.ts carry job_value_cents and timezone; all new props typed).
- `cd apps/web && npm run build` passes.
- Migration 015 re-run safety: the CHECK is DO-block guarded, the columns use `if not exists`, and the grant is naturally idempotent.
- Demo seed sanity (by eye — no local DB): delete-then-insert keyed on slug, is_active=false, all-fake 555 data, ~24 leads / ~14 calls / few sms, job_value_cents only on completed leads.
- Confirmed by inspection: is_active=true filters in app/(landing)/[clientSlug]/(page & [service]/page incl. generateStaticParams), app/sitemap.ts, and app/api/cron/monthly-reports/route.ts all exclude the is_active=false demo client.
</verification>

<success_criteria>
- Migration 015 adds leads.job_value_cents (guarded >=0 CHECK) + clients.timezone (default America/Chicago) + the timezone column grant, idempotently.
- Owners can set a per-lead job value (dollars in UI, cents in DB); ROI card and monthly report show real reported + estimated split with honest labels; leads table shows value for completed leads on large screens.
- Per-client timezone is selectable in settings and admin onboarding, persisted, used for the GHL sub-account, and applied to every dashboard timestamp; formatChicagoTime still works as a delegating wrapper.
- seed_demo.sql is a standalone, re-runnable, unmistakably-fake demo client that never surfaces on public pages, the sitemap, or monthly reports.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-ph8-demo-client-seed-real-job-value-tracking/260723-ph8-SUMMARY.md`.
</output>
