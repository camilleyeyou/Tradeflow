# Copy to .env and fill in values. Never commit .env.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
GHL_API_KEY=
GHL_LOCATION_ID=
CALLRAIL_API_KEY=
RESEND_API_KEY=
RESEND_FROM_EMAIL=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

ADMIN_EMAIL=

# --- Phase 2: GHL Webhook ---
# Note: GHL Ed25519 public key is hardcoded in api/services/ghl_service.py
# No additional env vars needed for webhook signature verification

# --- Phase 5: CallRail missed-call text-back (MISS-01..04) ---
# CallRail per-webhook signing secret for signature verification
CALLRAIL_WEBHOOK_SECRET=
# GHL sub-account private integration token used for text-back until
# per-client tokens land in Phase 6 (FIX-01)
GHL_PRIVATE_TOKEN=
# Pre-configured GHL text-back workflow id
GHL_SMS_WORKFLOW_ID=
